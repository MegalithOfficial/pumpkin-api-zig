// Generated by tools/gen.zig, do not edit.

const abi = @import("../abi.zig");
const world = @import("world.zig");
const player = @import("player.zig");
const block_entity = @import("block_entity.zig");
const common = @import("common.zig");
const item_stack = @import("item_stack.zig");
const server = @import("server.zig");
const text = @import("text.zig");

pub const World = world.World;
pub const Entity = world.Entity;
pub const Player = player.Player;
pub const CommandBlockEntity = block_entity.CommandBlockEntity;
pub const Position = common.Position;
pub const BlockPos = common.BlockPos;
pub const Locale = common.Locale;
pub const GameMode = common.GameMode;
pub const ItemStack = item_stack.ItemStack;
pub const Server = server.Server;
pub const Difficulty = server.Difficulty;
pub const TextComponent = text.TextComponent;

pub const CommandError = union(enum) {
    invalid_consumption: ?[]const u8,
    invalid_requirement,
    permission_denied,
    command_failed: TextComponent,
};

pub const CommandNode = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]command-node";

    pub fn literal(name: []const u8) CommandNode {
        return abi.call(raw.@"[static]command-node.literal", CommandNode, .{name});
    }

    pub fn argument(name: []const u8, @"type": ArgumentType) CommandNode {
        return abi.call(raw.@"[static]command-node.argument", CommandNode, .{ name, @"type" });
    }

    /// Consumes `node`.
    pub fn then(self: CommandNode, node: CommandNode) void {
        return abi.call(raw.@"[method]command-node.then", void, .{ abi.borrow(self), node });
    }

    pub fn executeWithHandlerId(self: CommandNode, handler_id: u32) void {
        return abi.call(raw.@"[method]command-node.execute-with-handler-id", void, .{ abi.borrow(self), handler_id });
    }

    pub fn suggestWithHandlerId(self: CommandNode, handler_id: u32) void {
        return abi.call(raw.@"[method]command-node.suggest-with-handler-id", void, .{ abi.borrow(self), handler_id });
    }

    pub fn requireWithHandlerId(self: CommandNode, handler_id: u32) void {
        return abi.call(raw.@"[method]command-node.require-with-handler-id", void, .{ abi.borrow(self), handler_id });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: CommandNode) void {
        abi.keep(CommandNode, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: CommandNode) void {
        abi.drop(CommandNode, self);
    }
};

pub const SuggestionRequest = struct {
    input: []const u8,
    cursor: u32,
    start: u32,
    remaining: []const u8,
};

pub const CommandSuggestion = struct {
    value: []const u8,
    tooltip: ?TextComponent,
};

pub const CommandSuggestions = struct {
    start: u32,
    length: u32,
    values: []const CommandSuggestion,
};

pub const EntityArgument = struct {
    single: bool,
    players_only: bool,
};

pub const CommandSenderType = union(enum) {
    rcon,
    console,
    player: Player,
    command_block: struct { CommandBlockEntity, World },
    dummy,
};

pub const PermissionLevel = enum(u8) {
    zero,
    one,
    two,
    three,
    four,
};

pub const CommandSender = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]command-sender";

    pub fn getCommandSenderType(self: CommandSender) CommandSenderType {
        return abi.call(raw.@"[method]command-sender.get-command-sender-type", CommandSenderType, .{abi.borrow(self)});
    }

    pub fn getName(self: CommandSender) []const u8 {
        return abi.call(raw.@"[method]command-sender.get-name", []const u8, .{abi.borrow(self)});
    }

    /// Consumes `text_`.
    pub fn sendMessage(self: CommandSender, text_: TextComponent) void {
        return abi.call(raw.@"[method]command-sender.send-message", void, .{ abi.borrow(self), text_ });
    }

    /// Consumes `text_`.
    pub fn sendSystemMessage(self: CommandSender, text_: TextComponent) void {
        return abi.call(raw.@"[method]command-sender.send-system-message", void, .{ abi.borrow(self), text_ });
    }

    /// Consumes `text_`.
    pub fn sendError(self: CommandSender, text_: TextComponent) void {
        return abi.call(raw.@"[method]command-sender.send-error", void, .{ abi.borrow(self), text_ });
    }

    pub fn setSuccessCount(self: CommandSender, count: i32) void {
        return abi.call(raw.@"[method]command-sender.set-success-count", void, .{ abi.borrow(self), count });
    }

    pub fn isPlayer(self: CommandSender) bool {
        return abi.call(raw.@"[method]command-sender.is-player", bool, .{abi.borrow(self)});
    }

    pub fn isConsole(self: CommandSender) bool {
        return abi.call(raw.@"[method]command-sender.is-console", bool, .{abi.borrow(self)});
    }

    pub fn asPlayer(self: CommandSender) ?Player {
        return abi.call(raw.@"[method]command-sender.as-player", ?Player, .{abi.borrow(self)});
    }

    pub fn permissionLevel(self: CommandSender) PermissionLevel {
        return abi.call(raw.@"[method]command-sender.permission-level", PermissionLevel, .{abi.borrow(self)});
    }

    pub fn hasPermissionLevel(self: CommandSender, level: PermissionLevel) bool {
        return abi.call(raw.@"[method]command-sender.has-permission-level", bool, .{ abi.borrow(self), level });
    }

    pub fn hasPermission(self: CommandSender, server_: Server, node: []const u8) bool {
        return abi.call(raw.@"[method]command-sender.has-permission", bool, .{ abi.borrow(self), abi.borrow(server_), node });
    }

    pub fn position(self: CommandSender) ?Position {
        return abi.call(raw.@"[method]command-sender.position", ?Position, .{abi.borrow(self)});
    }

    pub fn world(self: CommandSender) ?World {
        return abi.call(raw.@"[method]command-sender.world", ?World, .{abi.borrow(self)});
    }

    pub fn getLocale(self: CommandSender) Locale {
        return abi.call(raw.@"[method]command-sender.get-locale", Locale, .{abi.borrow(self)});
    }

    pub fn shouldReceiveFeedback(self: CommandSender) bool {
        return abi.call(raw.@"[method]command-sender.should-receive-feedback", bool, .{abi.borrow(self)});
    }

    pub fn shouldBroadcastConsoleToOps(self: CommandSender) bool {
        return abi.call(raw.@"[method]command-sender.should-broadcast-console-to-ops", bool, .{abi.borrow(self)});
    }

    pub fn shouldTrackOutput(self: CommandSender) bool {
        return abi.call(raw.@"[method]command-sender.should-track-output", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: CommandSender) void {
        abi.keep(CommandSender, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: CommandSender) void {
        abi.drop(CommandSender, self);
    }
};

pub const StringType = enum(u8) {
    single_word,
    quotable,
    greedy,
};

pub const ArgumentType = union(enum) {
    bool,
    float: struct { ?f32, ?f32 },
    double: struct { ?f64, ?f64 },
    integer: struct { ?i32, ?i32 },
    long: struct { ?i64, ?i64 },
    string: StringType,
    entities,
    entity,
    players,
    game_profile,
    block_pos,
    column_pos,
    position3d,
    position2d,
    block_state,
    block_predicate,
    item,
    item_predicate,
    color,
    component,
    style,
    message,
    nbt_compound_tag,
    nbt_tag,
    nbt_path,
    objective,
    objective_criteria,
    operation,
    particle,
    angle,
    rotation,
    scoreboard_slot,
    score_holder: bool,
    swizzle,
    team,
    item_slot,
    resource_location,
    mob_effect,
    function,
    entity_anchor,
    int_range,
    float_range,
    dimension,
    gamemode,
    difficulty,
    time: ?i32,
    resource: []const u8,
    resource_or_tag: []const u8,
    resource_or_tag_key: []const u8,
    resource_key: []const u8,
    template_mirror,
    template_rotation,
    uuid,
};

pub const SuggestionType = enum(u8) {
    ask_server,
    all_recipes,
    available_sounds,
    available_biomes,
    summonable_entities,
};

pub const BossbarColor = enum(u8) {
    pink,
    blue,
    red,
    green,
    yellow,
    purple,
    white,
};

pub const BossbarStyle = enum(u8) {
    no_division,
    notches6,
    notches10,
    notches12,
    notches20,
};

pub const SoundCategory = enum(u8) {
    master,
    music,
    records,
    weather,
    blocks,
    hostile,
    neutral,
    players,
    ambient,
    voice,
};

pub const EntityAnchor = enum(u8) {
    eyes,
    feet,
};

pub const Number = union(enum) {
    float64: f64,
    float32: f32,
    int32: i32,
    int64: i64,
};

pub const NotInBounds = union(enum) {
    lower_bound: struct { Number, Number },
    upper_bound: struct { Number, Number },
};

pub const Arg = union(enum) {
    simple: []const u8,
    msg: []const u8,
    bool: bool,
    item: []const u8,
    item_predicate: []const u8,
    resource_location: []const u8,
    block: []const u8,
    block_predicate: []const u8,
    time: i32,
    num: abi.Result(Number, NotInBounds),
    block_pos: BlockPos,
    pos3d: struct { f64, f64, f64 },
    pos2d: struct { f64, f64 },
    rotation: struct { f32, bool, f32, bool },
    gamemode: GameMode,
    difficulty: Difficulty,
    players: []const Player,
    particle: []const u8,
    text_component: TextComponent,
    bossbar_color: BossbarColor,
    bossbar_style: BossbarStyle,
    sound_category: SoundCategory,
    damage_type: []const u8,
    effect: []const u8,
    enchantment: []const u8,
    advancement: []const u8,
    entity_anchor: EntityAnchor,
};

pub const ConsumedArgs = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]consumed-args";

    pub fn getValue(self: ConsumedArgs, key: []const u8) Arg {
        return abi.call(raw.@"[method]consumed-args.get-value", Arg, .{ abi.borrow(self), key });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ConsumedArgs) void {
        abi.keep(ConsumedArgs, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ConsumedArgs) void {
        abi.drop(ConsumedArgs, self);
    }
};

pub const Command = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]command";

    /// First name is primary, rest are aliases
    pub fn init(names: []const []const u8, description: []const u8) Command {
        return abi.call(raw.@"[constructor]command", Command, .{ names, description });
    }

    /// Consumes `node`.
    pub fn then(self: Command, node: CommandNode) void {
        return abi.call(raw.@"[method]command.then", void, .{ abi.borrow(self), node });
    }

    pub fn executeWithHandlerId(self: Command, handler_id: u32) void {
        return abi.call(raw.@"[method]command.execute-with-handler-id", void, .{ abi.borrow(self), handler_id });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: Command) void {
        abi.keep(Command, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: Command) void {
        abi.drop(Command, self);
    }
};

const raw = struct {
    extern "pumpkin:plugin/command@0.1.0" fn @"[static]command-node.literal"(i32, i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[static]command-node.argument"(i32, i32, i32, i32, i64, i32, i64) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-node.then"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-node.execute-with-handler-id"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-node.suggest-with-handler-id"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-node.require-with-handler-id"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.get-command-sender-type"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.get-name"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.send-message"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.send-system-message"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.send-error"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.set-success-count"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.is-player"(i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.is-console"(i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.as-player"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.permission-level"(i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.has-permission-level"(i32, i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.has-permission"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.position"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.world"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.get-locale"(i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.should-receive-feedback"(i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.should-broadcast-console-to-ops"(i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command-sender.should-track-output"(i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]consumed-args.get-value"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[constructor]command"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command.then"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[method]command.execute-with-handler-id"(i32, i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[resource-drop]command-node"(i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[resource-drop]command-sender"(i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[resource-drop]consumed-args"(i32) void;
    extern "pumpkin:plugin/command@0.1.0" fn @"[resource-drop]command"(i32) void;
};
