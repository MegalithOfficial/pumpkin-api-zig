// Generated by tools/gen.zig, do not edit.

const abi = @import("../abi.zig");
const common = @import("common.zig");
const item_stack = @import("item_stack.zig");
const inventory = @import("inventory.zig");

pub const BlockPos = common.BlockPos;
pub const NbtTree = common.NbtTree;
pub const ItemStack = item_stack.ItemStack;
pub const Inventory = inventory.Inventory;

pub const BlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]block-entity";

    pub fn resourceLocation(self: BlockEntity) []const u8 {
        return abi.call(raw.@"[method]block-entity.resource-location", []const u8, .{abi.borrow(self)});
    }

    pub fn getPosition(self: BlockEntity) BlockPos {
        return abi.call(raw.@"[method]block-entity.get-position", BlockPos, .{abi.borrow(self)});
    }

    pub fn getId(self: BlockEntity) u32 {
        return abi.call(raw.@"[method]block-entity.get-id", u32, .{abi.borrow(self)});
    }

    pub fn isDirty(self: BlockEntity) bool {
        return abi.call(raw.@"[method]block-entity.is-dirty", bool, .{abi.borrow(self)});
    }

    pub fn clearDirty(self: BlockEntity) void {
        return abi.call(raw.@"[method]block-entity.clear-dirty", void, .{abi.borrow(self)});
    }

    /// Sets a namespaced custom data value on this block entity.
    pub fn setCustomData(self: BlockEntity, namespace: []const u8, key: []const u8, value: NbtTree) void {
        return abi.call(raw.@"[method]block-entity.set-custom-data", void, .{ abi.borrow(self), namespace, key, value });
    }

    /// Returns a namespaced custom data value from this block entity, if set.
    pub fn getCustomData(self: BlockEntity, namespace: []const u8, key: []const u8) ?NbtTree {
        return abi.call(raw.@"[method]block-entity.get-custom-data", ?NbtTree, .{ abi.borrow(self), namespace, key });
    }

    /// Removes a namespaced custom data value from this block entity.
    pub fn removeCustomData(self: BlockEntity, namespace: []const u8, key: []const u8) void {
        return abi.call(raw.@"[method]block-entity.remove-custom-data", void, .{ abi.borrow(self), namespace, key });
    }

    /// Returns whether this block entity has a namespaced custom data value.
    pub fn hasCustomData(self: BlockEntity, namespace: []const u8, key: []const u8) bool {
        return abi.call(raw.@"[method]block-entity.has-custom-data", bool, .{ abi.borrow(self), namespace, key });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BlockEntity) void {
        abi.keep(BlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BlockEntity) void {
        abi.drop(BlockEntity, self);
    }
};

pub const ContainerBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]container-block-entity";

    pub fn getBlockEntity(self: ContainerBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]container-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Returns the generic inventory handle for this container.
    pub fn getInventory(self: ContainerBlockEntity) Inventory {
        return abi.call(raw.@"[method]container-block-entity.get-inventory", Inventory, .{abi.borrow(self)});
    }

    pub fn getSize(self: ContainerBlockEntity) u32 {
        return abi.call(raw.@"[method]container-block-entity.get-size", u32, .{abi.borrow(self)});
    }

    pub fn isEmpty(self: ContainerBlockEntity) bool {
        return abi.call(raw.@"[method]container-block-entity.is-empty", bool, .{abi.borrow(self)});
    }

    pub fn getStack(self: ContainerBlockEntity, slot: u32) ?ItemStack {
        return abi.call(raw.@"[method]container-block-entity.get-stack", ?ItemStack, .{ abi.borrow(self), slot });
    }

    pub fn setStack(self: ContainerBlockEntity, slot: u32, stack: ?ItemStack) void {
        return abi.call(raw.@"[method]container-block-entity.set-stack", void, .{ abi.borrow(self), slot, stack });
    }

    pub fn removeStack(self: ContainerBlockEntity, slot: u32) ?ItemStack {
        return abi.call(raw.@"[method]container-block-entity.remove-stack", ?ItemStack, .{ abi.borrow(self), slot });
    }

    pub fn clear(self: ContainerBlockEntity) void {
        return abi.call(raw.@"[method]container-block-entity.clear", void, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ContainerBlockEntity) void {
        abi.keep(ContainerBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ContainerBlockEntity) void {
        abi.drop(ContainerBlockEntity, self);
    }
};

pub const CommandBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]command-block-entity";

    pub fn getBlockEntity(self: CommandBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]command-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn lastOutput(self: CommandBlockEntity) []const u8 {
        return abi.call(raw.@"[method]command-block-entity.last-output", []const u8, .{abi.borrow(self)});
    }

    pub fn trackOutput(self: CommandBlockEntity) bool {
        return abi.call(raw.@"[method]command-block-entity.track-output", bool, .{abi.borrow(self)});
    }

    pub fn successCount(self: CommandBlockEntity) u32 {
        return abi.call(raw.@"[method]command-block-entity.success-count", u32, .{abi.borrow(self)});
    }

    pub fn command(self: CommandBlockEntity) []const u8 {
        return abi.call(raw.@"[method]command-block-entity.command", []const u8, .{abi.borrow(self)});
    }

    pub fn auto(self: CommandBlockEntity) bool {
        return abi.call(raw.@"[method]command-block-entity.auto", bool, .{abi.borrow(self)});
    }

    pub fn conditionMet(self: CommandBlockEntity) bool {
        return abi.call(raw.@"[method]command-block-entity.condition-met", bool, .{abi.borrow(self)});
    }

    pub fn powered(self: CommandBlockEntity) bool {
        return abi.call(raw.@"[method]command-block-entity.powered", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: CommandBlockEntity) void {
        abi.keep(CommandBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: CommandBlockEntity) void {
        abi.drop(CommandBlockEntity, self);
    }
};

pub const DyeColor = enum(u8) {
    white,
    orange,
    magenta,
    light_blue,
    yellow,
    lime,
    pink,
    gray,
    light_gray,
    cyan,
    purple,
    blue,
    brown,
    green,
    red,
    black,
};

pub const SignText = struct {
    messages: []const []const u8,
    color: DyeColor,
    has_glowing_text: bool,
};

pub const SignBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]sign-block-entity";

    pub fn getBlockEntity(self: SignBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]sign-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getFrontText(self: SignBlockEntity) SignText {
        return abi.call(raw.@"[method]sign-block-entity.get-front-text", SignText, .{abi.borrow(self)});
    }

    pub fn setFrontText(self: SignBlockEntity, text: SignText) void {
        return abi.call(raw.@"[method]sign-block-entity.set-front-text", void, .{ abi.borrow(self), text });
    }

    pub fn getBackText(self: SignBlockEntity) SignText {
        return abi.call(raw.@"[method]sign-block-entity.get-back-text", SignText, .{abi.borrow(self)});
    }

    pub fn setBackText(self: SignBlockEntity, text: SignText) void {
        return abi.call(raw.@"[method]sign-block-entity.set-back-text", void, .{ abi.borrow(self), text });
    }

    pub fn isWaxed(self: SignBlockEntity) bool {
        return abi.call(raw.@"[method]sign-block-entity.is-waxed", bool, .{abi.borrow(self)});
    }

    pub fn setWaxed(self: SignBlockEntity, waxed: bool) void {
        return abi.call(raw.@"[method]sign-block-entity.set-waxed", void, .{ abi.borrow(self), waxed });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: SignBlockEntity) void {
        abi.keep(SignBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: SignBlockEntity) void {
        abi.drop(SignBlockEntity, self);
    }
};

pub const JukeboxBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]jukebox-block-entity";

    pub fn getBlockEntity(self: JukeboxBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]jukebox-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: JukeboxBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]jukebox-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn isPlaying(self: JukeboxBlockEntity) bool {
        return abi.call(raw.@"[method]jukebox-block-entity.is-playing", bool, .{abi.borrow(self)});
    }

    pub fn stopPlaying(self: JukeboxBlockEntity) void {
        return abi.call(raw.@"[method]jukebox-block-entity.stop-playing", void, .{abi.borrow(self)});
    }

    pub fn startPlaying(self: JukeboxBlockEntity, length_in_ticks: u64) void {
        return abi.call(raw.@"[method]jukebox-block-entity.start-playing", void, .{ abi.borrow(self), length_in_ticks });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: JukeboxBlockEntity) void {
        abi.keep(JukeboxBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: JukeboxBlockEntity) void {
        abi.drop(JukeboxBlockEntity, self);
    }
};

pub const ChestBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]chest-block-entity";

    pub fn getBlockEntity(self: ChestBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]chest-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: ChestBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]chest-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn viewerCount(self: ChestBlockEntity) u32 {
        return abi.call(raw.@"[method]chest-block-entity.viewer-count", u32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ChestBlockEntity) void {
        abi.keep(ChestBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ChestBlockEntity) void {
        abi.drop(ChestBlockEntity, self);
    }
};

pub const MobSpawnerBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]mob-spawner-block-entity";

    pub fn getBlockEntity(self: MobSpawnerBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]mob-spawner-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getSpawnCount(self: MobSpawnerBlockEntity) i32 {
        return abi.call(raw.@"[method]mob-spawner-block-entity.get-spawn-count", i32, .{abi.borrow(self)});
    }

    pub fn getSpawnRange(self: MobSpawnerBlockEntity) i32 {
        return abi.call(raw.@"[method]mob-spawner-block-entity.get-spawn-range", i32, .{abi.borrow(self)});
    }

    pub fn getDelay(self: MobSpawnerBlockEntity) i32 {
        return abi.call(raw.@"[method]mob-spawner-block-entity.get-delay", i32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: MobSpawnerBlockEntity) void {
        abi.keep(MobSpawnerBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: MobSpawnerBlockEntity) void {
        abi.drop(MobSpawnerBlockEntity, self);
    }
};

pub const MapBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]map-block-entity";

    pub fn getBlockEntity(self: MapBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]map-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getMapId(self: MapBlockEntity) i32 {
        return abi.call(raw.@"[method]map-block-entity.get-map-id", i32, .{abi.borrow(self)});
    }

    pub fn setMapId(self: MapBlockEntity, map_id: i32) void {
        return abi.call(raw.@"[method]map-block-entity.set-map-id", void, .{ abi.borrow(self), map_id });
    }

    pub fn getColors(self: MapBlockEntity) []const u8 {
        return abi.call(raw.@"[method]map-block-entity.get-colors", []const u8, .{abi.borrow(self)});
    }

    pub fn setColors(self: MapBlockEntity, colors: []const u8) void {
        return abi.call(raw.@"[method]map-block-entity.set-colors", void, .{ abi.borrow(self), colors });
    }

    pub fn setPixel(self: MapBlockEntity, x: u32, y: u32, color: u8) void {
        return abi.call(raw.@"[method]map-block-entity.set-pixel", void, .{ abi.borrow(self), x, y, color });
    }

    pub fn getPixel(self: MapBlockEntity, x: u32, y: u32) u8 {
        return abi.call(raw.@"[method]map-block-entity.get-pixel", u8, .{ abi.borrow(self), x, y });
    }

    pub fn update(self: MapBlockEntity) void {
        return abi.call(raw.@"[method]map-block-entity.update", void, .{abi.borrow(self)});
    }

    pub fn streamFrame(self: MapBlockEntity, frame_data: []const u8) void {
        return abi.call(raw.@"[method]map-block-entity.stream-frame", void, .{ abi.borrow(self), frame_data });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: MapBlockEntity) void {
        abi.keep(MapBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: MapBlockEntity) void {
        abi.drop(MapBlockEntity, self);
    }
};

pub const HangingSignBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]hanging-sign-block-entity";

    pub fn getBlockEntity(self: HangingSignBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]hanging-sign-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getFrontText(self: HangingSignBlockEntity) SignText {
        return abi.call(raw.@"[method]hanging-sign-block-entity.get-front-text", SignText, .{abi.borrow(self)});
    }

    pub fn setFrontText(self: HangingSignBlockEntity, text: SignText) void {
        return abi.call(raw.@"[method]hanging-sign-block-entity.set-front-text", void, .{ abi.borrow(self), text });
    }

    pub fn getBackText(self: HangingSignBlockEntity) SignText {
        return abi.call(raw.@"[method]hanging-sign-block-entity.get-back-text", SignText, .{abi.borrow(self)});
    }

    pub fn setBackText(self: HangingSignBlockEntity, text: SignText) void {
        return abi.call(raw.@"[method]hanging-sign-block-entity.set-back-text", void, .{ abi.borrow(self), text });
    }

    pub fn isWaxed(self: HangingSignBlockEntity) bool {
        return abi.call(raw.@"[method]hanging-sign-block-entity.is-waxed", bool, .{abi.borrow(self)});
    }

    pub fn setWaxed(self: HangingSignBlockEntity, waxed: bool) void {
        return abi.call(raw.@"[method]hanging-sign-block-entity.set-waxed", void, .{ abi.borrow(self), waxed });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: HangingSignBlockEntity) void {
        abi.keep(HangingSignBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: HangingSignBlockEntity) void {
        abi.drop(HangingSignBlockEntity, self);
    }
};

pub const TrappedChestBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]trapped-chest-block-entity";

    pub fn getBlockEntity(self: TrappedChestBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]trapped-chest-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: TrappedChestBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]trapped-chest-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn viewerCount(self: TrappedChestBlockEntity) u32 {
        return abi.call(raw.@"[method]trapped-chest-block-entity.viewer-count", u32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: TrappedChestBlockEntity) void {
        abi.keep(TrappedChestBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: TrappedChestBlockEntity) void {
        abi.drop(TrappedChestBlockEntity, self);
    }
};

pub const BannerBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]banner-block-entity";

    pub fn getBlockEntity(self: BannerBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]banner-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getCustomName(self: BannerBlockEntity) ?[]const u8 {
        return abi.call(raw.@"[method]banner-block-entity.get-custom-name", ?[]const u8, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BannerBlockEntity) void {
        abi.keep(BannerBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BannerBlockEntity) void {
        abi.drop(BannerBlockEntity, self);
    }
};

pub const BarrelBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]barrel-block-entity";

    pub fn getBlockEntity(self: BarrelBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]barrel-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: BarrelBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]barrel-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn viewerCount(self: BarrelBlockEntity) u32 {
        return abi.call(raw.@"[method]barrel-block-entity.viewer-count", u32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BarrelBlockEntity) void {
        abi.keep(BarrelBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BarrelBlockEntity) void {
        abi.drop(BarrelBlockEntity, self);
    }
};

pub const BeaconBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]beacon-block-entity";

    pub fn getBlockEntity(self: BeaconBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]beacon-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: BeaconBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]beacon-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getPrimaryEffect(self: BeaconBlockEntity) i32 {
        return abi.call(raw.@"[method]beacon-block-entity.get-primary-effect", i32, .{abi.borrow(self)});
    }

    pub fn getSecondaryEffect(self: BeaconBlockEntity) i32 {
        return abi.call(raw.@"[method]beacon-block-entity.get-secondary-effect", i32, .{abi.borrow(self)});
    }

    pub fn getLevels(self: BeaconBlockEntity) i32 {
        return abi.call(raw.@"[method]beacon-block-entity.get-levels", i32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BeaconBlockEntity) void {
        abi.keep(BeaconBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BeaconBlockEntity) void {
        abi.drop(BeaconBlockEntity, self);
    }
};

pub const BedBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]bed-block-entity";

    pub fn getBlockEntity(self: BedBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]bed-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BedBlockEntity) void {
        abi.keep(BedBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BedBlockEntity) void {
        abi.drop(BedBlockEntity, self);
    }
};

pub const BeehiveBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]beehive-block-entity";

    pub fn getBlockEntity(self: BeehiveBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]beehive-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getBeeCount(self: BeehiveBlockEntity) u32 {
        return abi.call(raw.@"[method]beehive-block-entity.get-bee-count", u32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BeehiveBlockEntity) void {
        abi.keep(BeehiveBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BeehiveBlockEntity) void {
        abi.drop(BeehiveBlockEntity, self);
    }
};

pub const BellBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]bell-block-entity";

    pub fn getBlockEntity(self: BellBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]bell-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn isRinging(self: BellBlockEntity) bool {
        return abi.call(raw.@"[method]bell-block-entity.is-ringing", bool, .{abi.borrow(self)});
    }

    pub fn getRingTicks(self: BellBlockEntity) i32 {
        return abi.call(raw.@"[method]bell-block-entity.get-ring-ticks", i32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BellBlockEntity) void {
        abi.keep(BellBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BellBlockEntity) void {
        abi.drop(BellBlockEntity, self);
    }
};

pub const BlastingFurnaceBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]blasting-furnace-block-entity";

    pub fn getBlockEntity(self: BlastingFurnaceBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]blasting-furnace-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: BlastingFurnaceBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]blasting-furnace-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getCookingTimeSpent(self: BlastingFurnaceBlockEntity) u16 {
        return abi.call(raw.@"[method]blasting-furnace-block-entity.get-cooking-time-spent", u16, .{abi.borrow(self)});
    }

    pub fn getCookingTotalTime(self: BlastingFurnaceBlockEntity) u16 {
        return abi.call(raw.@"[method]blasting-furnace-block-entity.get-cooking-total-time", u16, .{abi.borrow(self)});
    }

    pub fn getLitTimeRemaining(self: BlastingFurnaceBlockEntity) u16 {
        return abi.call(raw.@"[method]blasting-furnace-block-entity.get-lit-time-remaining", u16, .{abi.borrow(self)});
    }

    pub fn getLitTotalTime(self: BlastingFurnaceBlockEntity) u16 {
        return abi.call(raw.@"[method]blasting-furnace-block-entity.get-lit-total-time", u16, .{abi.borrow(self)});
    }

    pub fn isBurning(self: BlastingFurnaceBlockEntity) bool {
        return abi.call(raw.@"[method]blasting-furnace-block-entity.is-burning", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BlastingFurnaceBlockEntity) void {
        abi.keep(BlastingFurnaceBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BlastingFurnaceBlockEntity) void {
        abi.drop(BlastingFurnaceBlockEntity, self);
    }
};

pub const BrewingStandBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]brewing-stand-block-entity";

    pub fn getBlockEntity(self: BrewingStandBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]brewing-stand-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: BrewingStandBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]brewing-stand-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getBrewTime(self: BrewingStandBlockEntity) i32 {
        return abi.call(raw.@"[method]brewing-stand-block-entity.get-brew-time", i32, .{abi.borrow(self)});
    }

    pub fn getFuel(self: BrewingStandBlockEntity) i32 {
        return abi.call(raw.@"[method]brewing-stand-block-entity.get-fuel", i32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BrewingStandBlockEntity) void {
        abi.keep(BrewingStandBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BrewingStandBlockEntity) void {
        abi.drop(BrewingStandBlockEntity, self);
    }
};

pub const BrushableBlockBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]brushable-block-block-entity";

    pub fn getBlockEntity(self: BrushableBlockBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]brushable-block-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BrushableBlockBlockEntity) void {
        abi.keep(BrushableBlockBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BrushableBlockBlockEntity) void {
        abi.drop(BrushableBlockBlockEntity, self);
    }
};

pub const CalibratedSculkSensorBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]calibrated-sculk-sensor-block-entity";

    pub fn getBlockEntity(self: CalibratedSculkSensorBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]calibrated-sculk-sensor-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: CalibratedSculkSensorBlockEntity) void {
        abi.keep(CalibratedSculkSensorBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: CalibratedSculkSensorBlockEntity) void {
        abi.drop(CalibratedSculkSensorBlockEntity, self);
    }
};

pub const CampfireBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]campfire-block-entity";

    pub fn getBlockEntity(self: CampfireBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]campfire-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: CampfireBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]campfire-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: CampfireBlockEntity) void {
        abi.keep(CampfireBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: CampfireBlockEntity) void {
        abi.drop(CampfireBlockEntity, self);
    }
};

pub const ChiseledBookshelfBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]chiseled-bookshelf-block-entity";

    pub fn getBlockEntity(self: ChiseledBookshelfBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]chiseled-bookshelf-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: ChiseledBookshelfBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]chiseled-bookshelf-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getLastInteractedSlot(self: ChiseledBookshelfBlockEntity) i8 {
        return abi.call(raw.@"[method]chiseled-bookshelf-block-entity.get-last-interacted-slot", i8, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ChiseledBookshelfBlockEntity) void {
        abi.keep(ChiseledBookshelfBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ChiseledBookshelfBlockEntity) void {
        abi.drop(ChiseledBookshelfBlockEntity, self);
    }
};

pub const ComparatorBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]comparator-block-entity";

    pub fn getBlockEntity(self: ComparatorBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]comparator-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getOutputSignal(self: ComparatorBlockEntity) u8 {
        return abi.call(raw.@"[method]comparator-block-entity.get-output-signal", u8, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ComparatorBlockEntity) void {
        abi.keep(ComparatorBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ComparatorBlockEntity) void {
        abi.drop(ComparatorBlockEntity, self);
    }
};

pub const ConduitBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]conduit-block-entity";

    pub fn getBlockEntity(self: ConduitBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]conduit-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ConduitBlockEntity) void {
        abi.keep(ConduitBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ConduitBlockEntity) void {
        abi.drop(ConduitBlockEntity, self);
    }
};

pub const CopperGolemStatueBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]copper-golem-statue-block-entity";

    pub fn getBlockEntity(self: CopperGolemStatueBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]copper-golem-statue-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: CopperGolemStatueBlockEntity) void {
        abi.keep(CopperGolemStatueBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: CopperGolemStatueBlockEntity) void {
        abi.drop(CopperGolemStatueBlockEntity, self);
    }
};

pub const CrafterBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]crafter-block-entity";

    pub fn getBlockEntity(self: CrafterBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]crafter-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: CrafterBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]crafter-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getCraftingTicksRemaining(self: CrafterBlockEntity) i32 {
        return abi.call(raw.@"[method]crafter-block-entity.get-crafting-ticks-remaining", i32, .{abi.borrow(self)});
    }

    pub fn isTriggered(self: CrafterBlockEntity) bool {
        return abi.call(raw.@"[method]crafter-block-entity.is-triggered", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: CrafterBlockEntity) void {
        abi.keep(CrafterBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: CrafterBlockEntity) void {
        abi.drop(CrafterBlockEntity, self);
    }
};

pub const CreakingHeartBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]creaking-heart-block-entity";

    pub fn getBlockEntity(self: CreakingHeartBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]creaking-heart-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getCreakingUuid(self: CreakingHeartBlockEntity) ?[]const u8 {
        return abi.call(raw.@"[method]creaking-heart-block-entity.get-creaking-uuid", ?[]const u8, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: CreakingHeartBlockEntity) void {
        abi.keep(CreakingHeartBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: CreakingHeartBlockEntity) void {
        abi.drop(CreakingHeartBlockEntity, self);
    }
};

pub const DaylightDetectorBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]daylight-detector-block-entity";

    pub fn getBlockEntity(self: DaylightDetectorBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]daylight-detector-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: DaylightDetectorBlockEntity) void {
        abi.keep(DaylightDetectorBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: DaylightDetectorBlockEntity) void {
        abi.drop(DaylightDetectorBlockEntity, self);
    }
};

pub const DecoratedPotBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]decorated-pot-block-entity";

    pub fn getBlockEntity(self: DecoratedPotBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]decorated-pot-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: DecoratedPotBlockEntity) void {
        abi.keep(DecoratedPotBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: DecoratedPotBlockEntity) void {
        abi.drop(DecoratedPotBlockEntity, self);
    }
};

pub const DispenserBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]dispenser-block-entity";

    pub fn getBlockEntity(self: DispenserBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]dispenser-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: DispenserBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]dispenser-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: DispenserBlockEntity) void {
        abi.keep(DispenserBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: DispenserBlockEntity) void {
        abi.drop(DispenserBlockEntity, self);
    }
};

pub const DropperBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]dropper-block-entity";

    pub fn getBlockEntity(self: DropperBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]dropper-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: DropperBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]dropper-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: DropperBlockEntity) void {
        abi.keep(DropperBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: DropperBlockEntity) void {
        abi.drop(DropperBlockEntity, self);
    }
};

pub const EnchantingTableBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]enchanting-table-block-entity";

    pub fn getBlockEntity(self: EnchantingTableBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]enchanting-table-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: EnchantingTableBlockEntity) void {
        abi.keep(EnchantingTableBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: EnchantingTableBlockEntity) void {
        abi.drop(EnchantingTableBlockEntity, self);
    }
};

pub const EndGatewayBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]end-gateway-block-entity";

    pub fn getBlockEntity(self: EndGatewayBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]end-gateway-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getAge(self: EndGatewayBlockEntity) i64 {
        return abi.call(raw.@"[method]end-gateway-block-entity.get-age", i64, .{abi.borrow(self)});
    }

    pub fn isExactTeleport(self: EndGatewayBlockEntity) bool {
        return abi.call(raw.@"[method]end-gateway-block-entity.is-exact-teleport", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: EndGatewayBlockEntity) void {
        abi.keep(EndGatewayBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: EndGatewayBlockEntity) void {
        abi.drop(EndGatewayBlockEntity, self);
    }
};

pub const EndPortalBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]end-portal-block-entity";

    pub fn getBlockEntity(self: EndPortalBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]end-portal-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: EndPortalBlockEntity) void {
        abi.keep(EndPortalBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: EndPortalBlockEntity) void {
        abi.drop(EndPortalBlockEntity, self);
    }
};

pub const EnderChestBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]ender-chest-block-entity";

    pub fn getBlockEntity(self: EnderChestBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]ender-chest-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn viewerCount(self: EnderChestBlockEntity) u32 {
        return abi.call(raw.@"[method]ender-chest-block-entity.viewer-count", u32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: EnderChestBlockEntity) void {
        abi.keep(EnderChestBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: EnderChestBlockEntity) void {
        abi.drop(EnderChestBlockEntity, self);
    }
};

pub const FurnaceBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]furnace-block-entity";

    pub fn getBlockEntity(self: FurnaceBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]furnace-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: FurnaceBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]furnace-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getCookingTimeSpent(self: FurnaceBlockEntity) u16 {
        return abi.call(raw.@"[method]furnace-block-entity.get-cooking-time-spent", u16, .{abi.borrow(self)});
    }

    pub fn getCookingTotalTime(self: FurnaceBlockEntity) u16 {
        return abi.call(raw.@"[method]furnace-block-entity.get-cooking-total-time", u16, .{abi.borrow(self)});
    }

    pub fn getLitTimeRemaining(self: FurnaceBlockEntity) u16 {
        return abi.call(raw.@"[method]furnace-block-entity.get-lit-time-remaining", u16, .{abi.borrow(self)});
    }

    pub fn getLitTotalTime(self: FurnaceBlockEntity) u16 {
        return abi.call(raw.@"[method]furnace-block-entity.get-lit-total-time", u16, .{abi.borrow(self)});
    }

    pub fn isBurning(self: FurnaceBlockEntity) bool {
        return abi.call(raw.@"[method]furnace-block-entity.is-burning", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: FurnaceBlockEntity) void {
        abi.keep(FurnaceBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: FurnaceBlockEntity) void {
        abi.drop(FurnaceBlockEntity, self);
    }
};

pub const HopperBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]hopper-block-entity";

    pub fn getBlockEntity(self: HopperBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]hopper-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: HopperBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]hopper-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getCooldown(self: HopperBlockEntity) i32 {
        return abi.call(raw.@"[method]hopper-block-entity.get-cooldown", i32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: HopperBlockEntity) void {
        abi.keep(HopperBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: HopperBlockEntity) void {
        abi.drop(HopperBlockEntity, self);
    }
};

pub const JigsawBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]jigsaw-block-entity";

    pub fn getBlockEntity(self: JigsawBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]jigsaw-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getName(self: JigsawBlockEntity) []const u8 {
        return abi.call(raw.@"[method]jigsaw-block-entity.get-name", []const u8, .{abi.borrow(self)});
    }

    pub fn getTarget(self: JigsawBlockEntity) []const u8 {
        return abi.call(raw.@"[method]jigsaw-block-entity.get-target", []const u8, .{abi.borrow(self)});
    }

    pub fn getPool(self: JigsawBlockEntity) []const u8 {
        return abi.call(raw.@"[method]jigsaw-block-entity.get-pool", []const u8, .{abi.borrow(self)});
    }

    pub fn getFinalState(self: JigsawBlockEntity) []const u8 {
        return abi.call(raw.@"[method]jigsaw-block-entity.get-final-state", []const u8, .{abi.borrow(self)});
    }

    pub fn getSelectionPriority(self: JigsawBlockEntity) i32 {
        return abi.call(raw.@"[method]jigsaw-block-entity.get-selection-priority", i32, .{abi.borrow(self)});
    }

    pub fn getPlacementPriority(self: JigsawBlockEntity) i32 {
        return abi.call(raw.@"[method]jigsaw-block-entity.get-placement-priority", i32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: JigsawBlockEntity) void {
        abi.keep(JigsawBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: JigsawBlockEntity) void {
        abi.drop(JigsawBlockEntity, self);
    }
};

pub const LecternBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]lectern-block-entity";

    pub fn getBlockEntity(self: LecternBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]lectern-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: LecternBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]lectern-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getPage(self: LecternBlockEntity) u32 {
        return abi.call(raw.@"[method]lectern-block-entity.get-page", u32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: LecternBlockEntity) void {
        abi.keep(LecternBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: LecternBlockEntity) void {
        abi.drop(LecternBlockEntity, self);
    }
};

pub const PistonBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]piston-block-entity";

    pub fn getBlockEntity(self: PistonBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]piston-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getProgress(self: PistonBlockEntity) f32 {
        return abi.call(raw.@"[method]piston-block-entity.get-progress", f32, .{abi.borrow(self)});
    }

    pub fn isExtending(self: PistonBlockEntity) bool {
        return abi.call(raw.@"[method]piston-block-entity.is-extending", bool, .{abi.borrow(self)});
    }

    pub fn isSource(self: PistonBlockEntity) bool {
        return abi.call(raw.@"[method]piston-block-entity.is-source", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: PistonBlockEntity) void {
        abi.keep(PistonBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: PistonBlockEntity) void {
        abi.drop(PistonBlockEntity, self);
    }
};

pub const PotentSulfurBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]potent-sulfur-block-entity";

    pub fn getBlockEntity(self: PotentSulfurBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]potent-sulfur-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: PotentSulfurBlockEntity) void {
        abi.keep(PotentSulfurBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: PotentSulfurBlockEntity) void {
        abi.drop(PotentSulfurBlockEntity, self);
    }
};

pub const SculkCatalystBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]sculk-catalyst-block-entity";

    pub fn getBlockEntity(self: SculkCatalystBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]sculk-catalyst-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: SculkCatalystBlockEntity) void {
        abi.keep(SculkCatalystBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: SculkCatalystBlockEntity) void {
        abi.drop(SculkCatalystBlockEntity, self);
    }
};

pub const SculkSensorBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]sculk-sensor-block-entity";

    pub fn getBlockEntity(self: SculkSensorBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]sculk-sensor-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: SculkSensorBlockEntity) void {
        abi.keep(SculkSensorBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: SculkSensorBlockEntity) void {
        abi.drop(SculkSensorBlockEntity, self);
    }
};

pub const SculkShriekerBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]sculk-shrieker-block-entity";

    pub fn getBlockEntity(self: SculkShriekerBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]sculk-shrieker-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getWarningLevel(self: SculkShriekerBlockEntity) i32 {
        return abi.call(raw.@"[method]sculk-shrieker-block-entity.get-warning-level", i32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: SculkShriekerBlockEntity) void {
        abi.keep(SculkShriekerBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: SculkShriekerBlockEntity) void {
        abi.drop(SculkShriekerBlockEntity, self);
    }
};

pub const ShelfBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]shelf-block-entity";

    pub fn getBlockEntity(self: ShelfBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]shelf-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: ShelfBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]shelf-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ShelfBlockEntity) void {
        abi.keep(ShelfBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ShelfBlockEntity) void {
        abi.drop(ShelfBlockEntity, self);
    }
};

pub const ShulkerBoxBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]shulker-box-block-entity";

    pub fn getBlockEntity(self: ShulkerBoxBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]shulker-box-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: ShulkerBoxBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]shulker-box-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn viewerCount(self: ShulkerBoxBlockEntity) u32 {
        return abi.call(raw.@"[method]shulker-box-block-entity.viewer-count", u32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ShulkerBoxBlockEntity) void {
        abi.keep(ShulkerBoxBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ShulkerBoxBlockEntity) void {
        abi.drop(ShulkerBoxBlockEntity, self);
    }
};

pub const SkullBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]skull-block-entity";

    pub fn getBlockEntity(self: SkullBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]skull-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getNoteBlockSound(self: SkullBlockEntity) ?[]const u8 {
        return abi.call(raw.@"[method]skull-block-entity.get-note-block-sound", ?[]const u8, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: SkullBlockEntity) void {
        abi.keep(SkullBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: SkullBlockEntity) void {
        abi.drop(SkullBlockEntity, self);
    }
};

pub const SmokerBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]smoker-block-entity";

    pub fn getBlockEntity(self: SmokerBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]smoker-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getContainer(self: SmokerBlockEntity) ContainerBlockEntity {
        return abi.call(raw.@"[method]smoker-block-entity.get-container", ContainerBlockEntity, .{abi.borrow(self)});
    }

    pub fn getCookingTimeSpent(self: SmokerBlockEntity) u16 {
        return abi.call(raw.@"[method]smoker-block-entity.get-cooking-time-spent", u16, .{abi.borrow(self)});
    }

    pub fn getCookingTotalTime(self: SmokerBlockEntity) u16 {
        return abi.call(raw.@"[method]smoker-block-entity.get-cooking-total-time", u16, .{abi.borrow(self)});
    }

    pub fn getLitTimeRemaining(self: SmokerBlockEntity) u16 {
        return abi.call(raw.@"[method]smoker-block-entity.get-lit-time-remaining", u16, .{abi.borrow(self)});
    }

    pub fn getLitTotalTime(self: SmokerBlockEntity) u16 {
        return abi.call(raw.@"[method]smoker-block-entity.get-lit-total-time", u16, .{abi.borrow(self)});
    }

    pub fn isBurning(self: SmokerBlockEntity) bool {
        return abi.call(raw.@"[method]smoker-block-entity.is-burning", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: SmokerBlockEntity) void {
        abi.keep(SmokerBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: SmokerBlockEntity) void {
        abi.drop(SmokerBlockEntity, self);
    }
};

pub const StructureBlockBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]structure-block-block-entity";

    pub fn getBlockEntity(self: StructureBlockBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]structure-block-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    pub fn getName(self: StructureBlockBlockEntity) []const u8 {
        return abi.call(raw.@"[method]structure-block-block-entity.get-name", []const u8, .{abi.borrow(self)});
    }

    pub fn getAuthor(self: StructureBlockBlockEntity) []const u8 {
        return abi.call(raw.@"[method]structure-block-block-entity.get-author", []const u8, .{abi.borrow(self)});
    }

    pub fn getMode(self: StructureBlockBlockEntity) []const u8 {
        return abi.call(raw.@"[method]structure-block-block-entity.get-mode", []const u8, .{abi.borrow(self)});
    }

    pub fn getIntegrity(self: StructureBlockBlockEntity) f32 {
        return abi.call(raw.@"[method]structure-block-block-entity.get-integrity", f32, .{abi.borrow(self)});
    }

    pub fn getSeed(self: StructureBlockBlockEntity) i64 {
        return abi.call(raw.@"[method]structure-block-block-entity.get-seed", i64, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: StructureBlockBlockEntity) void {
        abi.keep(StructureBlockBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: StructureBlockBlockEntity) void {
        abi.drop(StructureBlockBlockEntity, self);
    }
};

pub const TestBlockBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]test-block-block-entity";

    pub fn getBlockEntity(self: TestBlockBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]test-block-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: TestBlockBlockEntity) void {
        abi.keep(TestBlockBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: TestBlockBlockEntity) void {
        abi.drop(TestBlockBlockEntity, self);
    }
};

pub const TestInstanceBlockBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]test-instance-block-block-entity";

    pub fn getBlockEntity(self: TestInstanceBlockBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]test-instance-block-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: TestInstanceBlockBlockEntity) void {
        abi.keep(TestInstanceBlockBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: TestInstanceBlockBlockEntity) void {
        abi.drop(TestInstanceBlockBlockEntity, self);
    }
};

pub const TrialSpawnerBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]trial-spawner-block-entity";

    pub fn getBlockEntity(self: TrialSpawnerBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]trial-spawner-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: TrialSpawnerBlockEntity) void {
        abi.keep(TrialSpawnerBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: TrialSpawnerBlockEntity) void {
        abi.drop(TrialSpawnerBlockEntity, self);
    }
};

pub const VaultBlockEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]vault-block-entity";

    pub fn getBlockEntity(self: VaultBlockEntity) BlockEntity {
        return abi.call(raw.@"[method]vault-block-entity.get-block-entity", BlockEntity, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: VaultBlockEntity) void {
        abi.keep(VaultBlockEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: VaultBlockEntity) void {
        abi.drop(VaultBlockEntity, self);
    }
};

pub const BlockEntityType = union(enum) {
    command_block_entity: CommandBlockEntity,
    sign_block_entity: SignBlockEntity,
    hanging_sign_block_entity: HangingSignBlockEntity,
    jukebox_block_entity: JukeboxBlockEntity,
    chest_block_entity: ChestBlockEntity,
    trapped_chest_block_entity: TrappedChestBlockEntity,
    mob_spawner_block_entity: MobSpawnerBlockEntity,
    map_block_entity: MapBlockEntity,
    banner_block_entity: BannerBlockEntity,
    barrel_block_entity: BarrelBlockEntity,
    beacon_block_entity: BeaconBlockEntity,
    bed_block_entity: BedBlockEntity,
    beehive_block_entity: BeehiveBlockEntity,
    bell_block_entity: BellBlockEntity,
    blasting_furnace_block_entity: BlastingFurnaceBlockEntity,
    brewing_stand_block_entity: BrewingStandBlockEntity,
    brushable_block_block_entity: BrushableBlockBlockEntity,
    calibrated_sculk_sensor_block_entity: CalibratedSculkSensorBlockEntity,
    campfire_block_entity: CampfireBlockEntity,
    chiseled_bookshelf_block_entity: ChiseledBookshelfBlockEntity,
    comparator_block_entity: ComparatorBlockEntity,
    conduit_block_entity: ConduitBlockEntity,
    copper_golem_statue_block_entity: CopperGolemStatueBlockEntity,
    crafter_block_entity: CrafterBlockEntity,
    creaking_heart_block_entity: CreakingHeartBlockEntity,
    daylight_detector_block_entity: DaylightDetectorBlockEntity,
    decorated_pot_block_entity: DecoratedPotBlockEntity,
    dispenser_block_entity: DispenserBlockEntity,
    dropper_block_entity: DropperBlockEntity,
    enchanting_table_block_entity: EnchantingTableBlockEntity,
    end_gateway_block_entity: EndGatewayBlockEntity,
    end_portal_block_entity: EndPortalBlockEntity,
    ender_chest_block_entity: EnderChestBlockEntity,
    furnace_block_entity: FurnaceBlockEntity,
    hopper_block_entity: HopperBlockEntity,
    jigsaw_block_entity: JigsawBlockEntity,
    lectern_block_entity: LecternBlockEntity,
    piston_block_entity: PistonBlockEntity,
    potent_sulfur_block_entity: PotentSulfurBlockEntity,
    sculk_catalyst_block_entity: SculkCatalystBlockEntity,
    sculk_sensor_block_entity: SculkSensorBlockEntity,
    sculk_shrieker_block_entity: SculkShriekerBlockEntity,
    shelf_block_entity: ShelfBlockEntity,
    shulker_box_block_entity: ShulkerBoxBlockEntity,
    skull_block_entity: SkullBlockEntity,
    smoker_block_entity: SmokerBlockEntity,
    structure_block_block_entity: StructureBlockBlockEntity,
    test_block_block_entity: TestBlockBlockEntity,
    test_instance_block_block_entity: TestInstanceBlockBlockEntity,
    trial_spawner_block_entity: TrialSpawnerBlockEntity,
    vault_block_entity: VaultBlockEntity,
    container_block_entity: ContainerBlockEntity,
};

const raw = struct {
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.resource-location"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.get-position"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.get-id"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.is-dirty"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.clear-dirty"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.set-custom-data"(i32, i32, i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.get-custom-data"(i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.remove-custom-data"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]block-entity.has-custom-data"(i32, i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]container-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]container-block-entity.get-inventory"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]container-block-entity.get-size"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]container-block-entity.is-empty"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]container-block-entity.get-stack"(i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]container-block-entity.set-stack"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]container-block-entity.remove-stack"(i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]container-block-entity.clear"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]command-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]command-block-entity.last-output"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]command-block-entity.track-output"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]command-block-entity.success-count"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]command-block-entity.command"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]command-block-entity.auto"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]command-block-entity.condition-met"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]command-block-entity.powered"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sign-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sign-block-entity.get-front-text"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sign-block-entity.set-front-text"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sign-block-entity.get-back-text"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sign-block-entity.set-back-text"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sign-block-entity.is-waxed"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sign-block-entity.set-waxed"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jukebox-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jukebox-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jukebox-block-entity.is-playing"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jukebox-block-entity.stop-playing"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jukebox-block-entity.start-playing"(i32, i64) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]chest-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]chest-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]chest-block-entity.viewer-count"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]mob-spawner-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]mob-spawner-block-entity.get-spawn-count"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]mob-spawner-block-entity.get-spawn-range"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]mob-spawner-block-entity.get-delay"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.get-map-id"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.set-map-id"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.get-colors"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.set-colors"(i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.set-pixel"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.get-pixel"(i32, i32, i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.update"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]map-block-entity.stream-frame"(i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hanging-sign-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hanging-sign-block-entity.get-front-text"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hanging-sign-block-entity.set-front-text"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hanging-sign-block-entity.get-back-text"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hanging-sign-block-entity.set-back-text"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hanging-sign-block-entity.is-waxed"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hanging-sign-block-entity.set-waxed"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]trapped-chest-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]trapped-chest-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]trapped-chest-block-entity.viewer-count"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]banner-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]banner-block-entity.get-custom-name"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]barrel-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]barrel-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]barrel-block-entity.viewer-count"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]beacon-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]beacon-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]beacon-block-entity.get-primary-effect"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]beacon-block-entity.get-secondary-effect"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]beacon-block-entity.get-levels"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]bed-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]beehive-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]beehive-block-entity.get-bee-count"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]bell-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]bell-block-entity.is-ringing"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]bell-block-entity.get-ring-ticks"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]blasting-furnace-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]blasting-furnace-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]blasting-furnace-block-entity.get-cooking-time-spent"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]blasting-furnace-block-entity.get-cooking-total-time"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]blasting-furnace-block-entity.get-lit-time-remaining"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]blasting-furnace-block-entity.get-lit-total-time"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]blasting-furnace-block-entity.is-burning"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]brewing-stand-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]brewing-stand-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]brewing-stand-block-entity.get-brew-time"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]brewing-stand-block-entity.get-fuel"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]brushable-block-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]calibrated-sculk-sensor-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]campfire-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]campfire-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]chiseled-bookshelf-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]chiseled-bookshelf-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]chiseled-bookshelf-block-entity.get-last-interacted-slot"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]comparator-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]comparator-block-entity.get-output-signal"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]conduit-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]copper-golem-statue-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]crafter-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]crafter-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]crafter-block-entity.get-crafting-ticks-remaining"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]crafter-block-entity.is-triggered"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]creaking-heart-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]creaking-heart-block-entity.get-creaking-uuid"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]daylight-detector-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]decorated-pot-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]dispenser-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]dispenser-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]dropper-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]dropper-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]enchanting-table-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]end-gateway-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]end-gateway-block-entity.get-age"(i32) i64;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]end-gateway-block-entity.is-exact-teleport"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]end-portal-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]ender-chest-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]ender-chest-block-entity.viewer-count"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]furnace-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]furnace-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]furnace-block-entity.get-cooking-time-spent"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]furnace-block-entity.get-cooking-total-time"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]furnace-block-entity.get-lit-time-remaining"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]furnace-block-entity.get-lit-total-time"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]furnace-block-entity.is-burning"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hopper-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hopper-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]hopper-block-entity.get-cooldown"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jigsaw-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jigsaw-block-entity.get-name"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jigsaw-block-entity.get-target"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jigsaw-block-entity.get-pool"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jigsaw-block-entity.get-final-state"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jigsaw-block-entity.get-selection-priority"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]jigsaw-block-entity.get-placement-priority"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]lectern-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]lectern-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]lectern-block-entity.get-page"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]piston-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]piston-block-entity.get-progress"(i32) f32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]piston-block-entity.is-extending"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]piston-block-entity.is-source"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]potent-sulfur-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sculk-catalyst-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sculk-sensor-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sculk-shrieker-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]sculk-shrieker-block-entity.get-warning-level"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]shelf-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]shelf-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]shulker-box-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]shulker-box-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]shulker-box-block-entity.viewer-count"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]skull-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]skull-block-entity.get-note-block-sound"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]smoker-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]smoker-block-entity.get-container"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]smoker-block-entity.get-cooking-time-spent"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]smoker-block-entity.get-cooking-total-time"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]smoker-block-entity.get-lit-time-remaining"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]smoker-block-entity.get-lit-total-time"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]smoker-block-entity.is-burning"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]structure-block-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]structure-block-block-entity.get-name"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]structure-block-block-entity.get-author"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]structure-block-block-entity.get-mode"(i32, i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]structure-block-block-entity.get-integrity"(i32) f32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]structure-block-block-entity.get-seed"(i32) i64;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]test-block-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]test-instance-block-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]trial-spawner-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[method]vault-block-entity.get-block-entity"(i32) i32;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]container-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]command-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]sign-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]jukebox-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]chest-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]mob-spawner-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]map-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]hanging-sign-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]trapped-chest-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]banner-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]barrel-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]beacon-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]bed-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]beehive-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]bell-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]blasting-furnace-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]brewing-stand-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]brushable-block-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]calibrated-sculk-sensor-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]campfire-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]chiseled-bookshelf-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]comparator-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]conduit-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]copper-golem-statue-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]crafter-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]creaking-heart-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]daylight-detector-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]decorated-pot-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]dispenser-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]dropper-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]enchanting-table-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]end-gateway-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]end-portal-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]ender-chest-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]furnace-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]hopper-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]jigsaw-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]lectern-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]piston-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]potent-sulfur-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]sculk-catalyst-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]sculk-sensor-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]sculk-shrieker-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]shelf-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]shulker-box-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]skull-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]smoker-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]structure-block-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]test-block-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]test-instance-block-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]trial-spawner-block-entity"(i32) void;
    extern "pumpkin:plugin/block-entity@0.1.0" fn @"[resource-drop]vault-block-entity"(i32) void;
};
