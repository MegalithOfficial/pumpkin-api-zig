// Generated by tools/gen.zig, do not edit.

/// Serialized text component as a postcard byte array.
/// @deprecated Use the text-component resource from the text interface instead.
pub const RawTextComponent = []const u8;

/// Represents one entry in an NBT compound tag.
pub const NbtEntry = struct {
    key: []const u8,
    value: u32,
};

/// Represents a Minecraft NBT tag.
pub const NbtTag = union(enum) {
    byte: i8,
    short: i16,
    int: i32,
    long: i64,
    float: f32,
    double: f64,
    byte_array: []const i8,
    string_tag: []const u8,
    list_tag: []const u32,
    compound: []const NbtEntry,
    int_array: []const i32,
    long_array: []const i64,
};

/// Represents a Minecraft NBT tag tree.
pub const NbtTree = struct {
    root: u32,
    tags: []const NbtTag,
};

/// A position in the world grid (integers).
pub const BlockPos = struct {
    x: i32,
    y: i32,
    z: i32,
};

/// A precise coordinate in the world (floats).
pub const Position = struct { f64, f64, f64 };

/// Represents the hands of a player.
pub const Hand = enum(u8) {
    left,
    right,
};

/// Represents the game modes available in Minecraft.
pub const GameMode = enum(u8) {
    survival,
    creative,
    adventure,
    spectator,
};

/// The 16 base Minecraft colors.
pub const NamedColor = enum(u8) {
    black,
    dark_blue,
    dark_green,
    dark_aqua,
    dark_red,
    dark_purple,
    gold,
    gray,
    dark_gray,
    blue,
    green,
    aqua,
    red,
    light_purple,
    yellow,
    white,
};

/// An RGB color represented by its red, green, and blue components.
pub const RgbColor = struct {
    r: u8,
    g: u8,
    b: u8,
};

/// An ARGB color with alpha (transparency) channel.
pub const ArgbColor = struct {
    a: u8,
    r: u8,
    g: u8,
    b: u8,
};

/// Represents the type of click interaction in a GUI.
pub const ClickType = enum(u8) {
    left,
    right,
    shift_left,
    shift_right,
    middle,
    drop,
    control_drop,
    double_click,
    number_key,
    unknown,
};

/// Represents the various poses an entity can be in.
pub const EntityPose = enum(u8) {
    standing,
    fall_flying,
    sleeping,
    swimming,
    spin_attack,
    crouching,
    long_jumping,
    dying,
    croaking,
    using_tongue,
    sitting,
    roaring,
    sniffing,
    emerging,
    digging,
    sliding,
    shooting,
    inhaling,
};

/// All Minecraft-supported locales.
pub const Locale = enum(u8) {
    af_za,
    ar_sa,
    ast_es,
    az_az,
    ba_ru,
    bar,
    be_by,
    bg_bg,
    br_fr,
    brb,
    bs_ba,
    ca_es,
    cs_cz,
    cy_gb,
    da_dk,
    de_at,
    de_ch,
    de_de,
    el_gr,
    en_au,
    en_ca,
    en_gb,
    en_nz,
    en_pt,
    en_ud,
    en_us,
    enp,
    enws,
    eo_uy,
    es_ar,
    es_cl,
    es_ec,
    es_es,
    es_mx,
    es_uy,
    es_ve,
    esan,
    et_ee,
    eu_es,
    fa_ir,
    fi_fi,
    fil_ph,
    fo_fo,
    fr_ca,
    fr_fr,
    fra_de,
    fur_it,
    fy_nl,
    ga_ie,
    gd_gb,
    gl_es,
    haw_us,
    he_il,
    hi_in,
    hr_hr,
    hu_hu,
    hy_am,
    id_id,
    ig_ng,
    io_en,
    is_is,
    isv,
    it_it,
    ja_jp,
    jbo_en,
    ka_ge,
    kk_kz,
    kn_in,
    ko_kr,
    ksh,
    kw_gb,
    la_la,
    lb_lu,
    li_li,
    lmo,
    lo_la,
    lol_us,
    lt_lt,
    lv_lv,
    lzh,
    mk_mk,
    mn_mn,
    ms_my,
    mt_mt,
    nah,
    nds_de,
    nl_be,
    nl_nl,
    nn_no,
    no_no,
    oc_fr,
    ovd,
    pl_pl,
    pt_br,
    pt_pt,
    qya_aa,
    ro_ro,
    rpr,
    ru_ru,
    ry_ua,
    sah_sah,
    se_no,
    sk_sk,
    sl_si,
    so_so,
    sq_al,
    sr_cs,
    sr_sp,
    sv_se,
    sxu,
    szl,
    ta_in,
    th_th,
    tl_ph,
    tlh_aa,
    tok,
    tr_tr,
    tt_ru,
    uk_ua,
    val_es,
    vec_it,
    vi_vn,
    yi_de,
    yo_ng,
    zh_cn,
    zh_hk,
    zh_tw,
    zlm_arab,
};
