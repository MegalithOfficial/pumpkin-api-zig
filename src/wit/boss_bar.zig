// Generated by tools/gen.zig, do not edit.

const abi = @import("../abi.zig");
const text = @import("text.zig");
const player = @import("player.zig");

pub const TextComponent = text.TextComponent;
pub const Player = player.Player;

/// Represents the color of a boss bar.
pub const BossBarColor = enum(u8) {
    pink,
    blue,
    red,
    green,
    yellow,
    purple,
    white,
};

/// Represents the notches/divisions of a boss bar.
pub const BossBarDivision = enum(u8) {
    no_division,
    notches_6,
    notches_10,
    notches_12,
    notches_20,
};

/// Metadata flags that can be toggled on a boss bar.
pub const BossBarMetadata = struct {
    /// Darkens the sky for players who see the boss bar.
    darken_sky: bool,
    /// If true, the bar is considered a "dragon bar" (used for Ender Dragon).
    dragon_bar: bool,
    /// If true, fog is created around the players who see the boss bar.
    create_fog: bool,
};

/// Represents a boss bar that can be shown to players.
pub const BossBar = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]boss-bar";

    /// Creates a new boss bar.
    ///
    /// Consumes `title`.
    pub fn init(title: TextComponent, color: BossBarColor, division: BossBarDivision) BossBar {
        return abi.call(raw.@"[constructor]boss-bar", BossBar, .{ title, color, division });
    }

    /// Returns the title of this boss bar.
    pub fn getTitle(self: BossBar) TextComponent {
        return abi.call(raw.@"[method]boss-bar.get-title", TextComponent, .{abi.borrow(self)});
    }

    /// Sets the title of this boss bar.
    ///
    /// Consumes `title`.
    pub fn setTitle(self: BossBar, title: TextComponent) void {
        return abi.call(raw.@"[method]boss-bar.set-title", void, .{ abi.borrow(self), title });
    }

    /// Returns the health (0.0 to 1.0) of this boss bar.
    pub fn getHealth(self: BossBar) f32 {
        return abi.call(raw.@"[method]boss-bar.get-health", f32, .{abi.borrow(self)});
    }

    /// Sets the health (0.0 to 1.0) of this boss bar.
    pub fn setHealth(self: BossBar, health: f32) void {
        return abi.call(raw.@"[method]boss-bar.set-health", void, .{ abi.borrow(self), health });
    }

    /// Returns the color of this boss bar.
    pub fn getColor(self: BossBar) BossBarColor {
        return abi.call(raw.@"[method]boss-bar.get-color", BossBarColor, .{abi.borrow(self)});
    }

    /// Sets the color of this boss bar.
    pub fn setColor(self: BossBar, color: BossBarColor) void {
        return abi.call(raw.@"[method]boss-bar.set-color", void, .{ abi.borrow(self), color });
    }

    /// Returns the division of this boss bar.
    pub fn getDivision(self: BossBar) BossBarDivision {
        return abi.call(raw.@"[method]boss-bar.get-division", BossBarDivision, .{abi.borrow(self)});
    }

    /// Sets the division of this boss bar.
    pub fn setDivision(self: BossBar, division: BossBarDivision) void {
        return abi.call(raw.@"[method]boss-bar.set-division", void, .{ abi.borrow(self), division });
    }

    /// Returns the currently active metadata on this boss bar.
    pub fn getMetadata(self: BossBar) BossBarMetadata {
        return abi.call(raw.@"[method]boss-bar.get-metadata", BossBarMetadata, .{abi.borrow(self)});
    }

    /// Updates the metadata on this boss bar.
    pub fn setMetadata(self: BossBar, metadata: BossBarMetadata) void {
        return abi.call(raw.@"[method]boss-bar.set-metadata", void, .{ abi.borrow(self), metadata });
    }

    /// Adds a player to this boss bar, making it visible to them.
    ///
    /// Consumes `player_`.
    pub fn addPlayer(self: BossBar, player_: Player) void {
        return abi.call(raw.@"[method]boss-bar.add-player", void, .{ abi.borrow(self), player_ });
    }

    /// Removes a player from this boss bar, hiding it from them.
    ///
    /// Consumes `player_`.
    pub fn removePlayer(self: BossBar, player_: Player) void {
        return abi.call(raw.@"[method]boss-bar.remove-player", void, .{ abi.borrow(self), player_ });
    }

    /// Returns a list of all players currently seeing this boss bar.
    pub fn getPlayers(self: BossBar) []const Player {
        return abi.call(raw.@"[method]boss-bar.get-players", []const Player, .{abi.borrow(self)});
    }

    /// Removes this boss bar from all players and cleans it up.
    pub fn removeAll(self: BossBar) void {
        return abi.call(raw.@"[method]boss-bar.remove-all", void, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: BossBar) void {
        abi.keep(BossBar, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: BossBar) void {
        abi.drop(BossBar, self);
    }
};

const raw = struct {
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[constructor]boss-bar"(i32, i32, i32) i32;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.get-title"(i32) i32;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.set-title"(i32, i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.get-health"(i32) f32;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.set-health"(i32, f32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.get-color"(i32) i32;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.set-color"(i32, i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.get-division"(i32) i32;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.set-division"(i32, i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.get-metadata"(i32, i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.set-metadata"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.add-player"(i32, i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.remove-player"(i32, i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.get-players"(i32, i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[method]boss-bar.remove-all"(i32) void;
    extern "pumpkin:plugin/boss-bar@0.1.0" fn @"[resource-drop]boss-bar"(i32) void;
};
