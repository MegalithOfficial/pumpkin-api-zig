// Generated by tools/gen.zig, do not edit.

const abi = @import("../abi.zig");
const attributes = @import("attributes.zig");
const data_components = @import("data_components.zig");
const enchantments = @import("enchantments.zig");
const text = @import("text.zig");
const common = @import("common.zig");

pub const Attribute = attributes.Attribute;
pub const AttributeModifier = attributes.AttributeModifier;
pub const DataComponent = data_components.DataComponent;
pub const AttributeModifierSlot = enchantments.AttributeModifierSlot;
pub const Enchantment = enchantments.Enchantment;
pub const TextComponent = text.TextComponent;
pub const NbtEntry = common.NbtEntry;
pub const NbtTree = common.NbtTree;
pub const NbtTag = common.NbtTag;

/// Represents an enchantment and its level.
pub const EnchantmentValue = struct {
    enchantment: Enchantment,
    level: u32,
};

/// Represents a custom enchantment and its level on an item stack.
pub const CustomEnchantmentValue = struct {
    enchantment_id: []const u8,
    level: u32,
};

/// Represents an attribute modifier applied to an item stack for a specific slot.
pub const ItemAttributeModifier = struct {
    attribute: Attribute,
    modifier: AttributeModifier,
    slot: AttributeModifierSlot,
};

/// Represents a data component and its serialized value.
pub const DataComponentValue = struct {
    component: DataComponent,
    /// The serialized value of the component.
    value: []const u8,
};

pub const ItemStack = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]item-stack";

    pub fn init(registry_key: []const u8, count: u8) ItemStack {
        return abi.call(raw.@"[constructor]item-stack", ItemStack, .{ registry_key, count });
    }

    /// Returns the unique registry key of the item (e.g., "minecraft:diamond").
    pub fn getRegistryKey(self: ItemStack) []const u8 {
        return abi.call(raw.@"[method]item-stack.get-registry-key", []const u8, .{abi.borrow(self)});
    }

    /// Returns the number of items in the stack.
    pub fn getCount(self: ItemStack) u8 {
        return abi.call(raw.@"[method]item-stack.get-count", u8, .{abi.borrow(self)});
    }

    /// Sets the number of items in the stack.
    pub fn setCount(self: ItemStack, count: u8) void {
        return abi.call(raw.@"[method]item-stack.set-count", void, .{ abi.borrow(self), count });
    }

    /// Returns the maximum number of items that can be in this stack.
    pub fn getMaxCount(self: ItemStack) u8 {
        return abi.call(raw.@"[method]item-stack.get-max-count", u8, .{abi.borrow(self)});
    }

    /// Returns all enchantments on this item stack.
    pub fn getEnchantments(self: ItemStack) []const EnchantmentValue {
        return abi.call(raw.@"[method]item-stack.get-enchantments", []const EnchantmentValue, .{abi.borrow(self)});
    }

    /// Adds an enchantment to this item stack.
    pub fn addEnchantment(self: ItemStack, enchantment: Enchantment, level: u32) void {
        return abi.call(raw.@"[method]item-stack.add-enchantment", void, .{ abi.borrow(self), enchantment, level });
    }

    /// Removes an enchantment from this item stack.
    pub fn removeEnchantment(self: ItemStack, enchantment: Enchantment) void {
        return abi.call(raw.@"[method]item-stack.remove-enchantment", void, .{ abi.borrow(self), enchantment });
    }

    /// Returns all custom enchantments on this item stack.
    pub fn getCustomEnchantments(self: ItemStack) []const CustomEnchantmentValue {
        return abi.call(raw.@"[method]item-stack.get-custom-enchantments", []const CustomEnchantmentValue, .{abi.borrow(self)});
    }

    /// Adds a custom enchantment to this item stack.
    pub fn addCustomEnchantment(self: ItemStack, enchantment_id: []const u8, level: u32) void {
        return abi.call(raw.@"[method]item-stack.add-custom-enchantment", void, .{ abi.borrow(self), enchantment_id, level });
    }

    /// Removes a custom enchantment from this item stack.
    pub fn removeCustomEnchantment(self: ItemStack, enchantment_id: []const u8) void {
        return abi.call(raw.@"[method]item-stack.remove-custom-enchantment", void, .{ abi.borrow(self), enchantment_id });
    }

    /// Returns the level of a custom enchantment on this item stack, if present.
    pub fn getCustomEnchantmentLevel(self: ItemStack, enchantment_id: []const u8) ?u32 {
        return abi.call(raw.@"[method]item-stack.get-custom-enchantment-level", ?u32, .{ abi.borrow(self), enchantment_id });
    }

    /// Returns whether this item stack has a specific custom enchantment.
    pub fn hasCustomEnchantment(self: ItemStack, enchantment_id: []const u8) bool {
        return abi.call(raw.@"[method]item-stack.has-custom-enchantment", bool, .{ abi.borrow(self), enchantment_id });
    }

    /// Returns all attribute modifiers on this item stack.
    pub fn getAttributeModifiers(self: ItemStack) []const ItemAttributeModifier {
        return abi.call(raw.@"[method]item-stack.get-attribute-modifiers", []const ItemAttributeModifier, .{abi.borrow(self)});
    }

    /// Adds an attribute modifier to this item stack.
    pub fn addAttributeModifier(self: ItemStack, modifier: ItemAttributeModifier) void {
        return abi.call(raw.@"[method]item-stack.add-attribute-modifier", void, .{ abi.borrow(self), modifier });
    }

    /// Removes all attribute modifiers for a specific attribute from this item stack.
    pub fn removeAttributeModifiers(self: ItemStack, attribute: Attribute) void {
        return abi.call(raw.@"[method]item-stack.remove-attribute-modifiers", void, .{ abi.borrow(self), attribute });
    }

    /// Clears all custom attribute modifiers from this item stack.
    pub fn clearAttributeModifiers(self: ItemStack) void {
        return abi.call(raw.@"[method]item-stack.clear-attribute-modifiers", void, .{abi.borrow(self)});
    }

    /// Returns the lore of this item stack.
    pub fn getLore(self: ItemStack) []const TextComponent {
        return abi.call(raw.@"[method]item-stack.get-lore", []const TextComponent, .{abi.borrow(self)});
    }

    /// Sets the lore of this item stack.
    pub fn setLore(self: ItemStack, lore: []const TextComponent) void {
        return abi.call(raw.@"[method]item-stack.set-lore", void, .{ abi.borrow(self), lore });
    }

    /// Adds a line to the lore of this item stack.
    ///
    /// Consumes `line`.
    pub fn addLore(self: ItemStack, line: TextComponent) void {
        return abi.call(raw.@"[method]item-stack.add-lore", void, .{ abi.borrow(self), line });
    }

    /// Returns the custom name of this item stack, if set.
    pub fn getCustomName(self: ItemStack) ?TextComponent {
        return abi.call(raw.@"[method]item-stack.get-custom-name", ?TextComponent, .{abi.borrow(self)});
    }

    /// Sets the custom name of this item stack.
    pub fn setCustomName(self: ItemStack, name: ?TextComponent) void {
        return abi.call(raw.@"[method]item-stack.set-custom-name", void, .{ abi.borrow(self), name });
    }

    /// Sets a namespaced custom data value on this item stack.
    pub fn setCustomData(self: ItemStack, namespace: []const u8, key: []const u8, value: NbtTree) void {
        return abi.call(raw.@"[method]item-stack.set-custom-data", void, .{ abi.borrow(self), namespace, key, value });
    }

    /// Returns a namespaced custom data value from this item stack, if set.
    pub fn getCustomData(self: ItemStack, namespace: []const u8, key: []const u8) ?NbtTree {
        return abi.call(raw.@"[method]item-stack.get-custom-data", ?NbtTree, .{ abi.borrow(self), namespace, key });
    }

    /// Removes a namespaced custom data value from this item stack.
    pub fn removeCustomData(self: ItemStack, namespace: []const u8, key: []const u8) void {
        return abi.call(raw.@"[method]item-stack.remove-custom-data", void, .{ abi.borrow(self), namespace, key });
    }

    /// Returns whether this item stack has a namespaced custom data value.
    pub fn hasCustomData(self: ItemStack, namespace: []const u8, key: []const u8) bool {
        return abi.call(raw.@"[method]item-stack.has-custom-data", bool, .{ abi.borrow(self), namespace, key });
    }

    /// Returns all data components on this item stack.
    pub fn getComponents(self: ItemStack) []const DataComponentValue {
        return abi.call(raw.@"[method]item-stack.get-components", []const DataComponentValue, .{abi.borrow(self)});
    }

    /// Sets a data component on this item stack.
    pub fn setComponent(self: ItemStack, component: DataComponent, value: []const u8) void {
        return abi.call(raw.@"[method]item-stack.set-component", void, .{ abi.borrow(self), component, value });
    }

    /// Removes a data component from this item stack.
    pub fn removeComponent(self: ItemStack, component: DataComponent) void {
        return abi.call(raw.@"[method]item-stack.remove-component", void, .{ abi.borrow(self), component });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ItemStack) void {
        abi.keep(ItemStack, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ItemStack) void {
        abi.drop(ItemStack, self);
    }
};

const raw = struct {
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[constructor]item-stack"(i32, i32, i32) i32;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-registry-key"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-count"(i32) i32;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.set-count"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-max-count"(i32) i32;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-enchantments"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.add-enchantment"(i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.remove-enchantment"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-custom-enchantments"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.add-custom-enchantment"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.remove-custom-enchantment"(i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-custom-enchantment-level"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.has-custom-enchantment"(i32, i32, i32) i32;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-attribute-modifiers"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.add-attribute-modifier"(i32, i32, i32, i32, f64, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.remove-attribute-modifiers"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.clear-attribute-modifiers"(i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-lore"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.set-lore"(i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.add-lore"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-custom-name"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.set-custom-name"(i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.set-custom-data"(i32, i32, i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-custom-data"(i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.remove-custom-data"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.has-custom-data"(i32, i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.get-components"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.set-component"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[method]item-stack.remove-component"(i32, i32) void;
    extern "pumpkin:plugin/item-stack@0.1.0" fn @"[resource-drop]item-stack"(i32) void;
};
