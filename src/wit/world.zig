// Generated by tools/gen.zig, do not edit.

const abi = @import("../abi.zig");
const common = @import("common.zig");
const block_entity = @import("block_entity.zig");
const text = @import("text.zig");
const scoreboard = @import("scoreboard.zig");
const particles = @import("particles.zig");
const sounds = @import("sounds.zig");
const entity_types = @import("entity_types.zig");
const biomes = @import("biomes.zig");
const attributes = @import("attributes.zig");
const item_stack = @import("item_stack.zig");
const damage_types = @import("damage_types.zig");
const game_rules = @import("game_rules.zig");
const uuid = @import("uuid.zig");

pub const Position = common.Position;
pub const EntityPose = common.EntityPose;
pub const BlockPos = common.BlockPos;
pub const NbtTree = common.NbtTree;
pub const BlockEntity = block_entity.BlockEntity;
pub const CommandBlockEntity = block_entity.CommandBlockEntity;
pub const BlockEntityType = block_entity.BlockEntityType;
pub const TextComponent = text.TextComponent;
pub const Scoreboard = scoreboard.Scoreboard;
pub const Particle = particles.Particle;
pub const Sound = sounds.Sound;
pub const SoundCategory = sounds.SoundCategory;
pub const EntityType = entity_types.EntityType;
pub const Biome = biomes.Biome;
pub const Attribute = attributes.Attribute;
pub const ModifierOperation = attributes.ModifierOperation;
pub const AttributeModifier = attributes.AttributeModifier;
pub const ItemStack = item_stack.ItemStack;
pub const DamageType = damage_types.DamageType;
pub const GameRule = game_rules.GameRule;
pub const GameRuleValue = game_rules.GameRuleValue;
pub const Uuid = uuid.Uuid;

/// Equipment slots available on living entities (mobs, armor stands, players).
pub const EquipmentSlot = enum(u8) {
    main_hand,
    off_hand,
    feet,
    legs,
    chest,
    head,
    body,
    saddle,
};

/// Defines how a block reacts when pushed by a piston.
pub const PistonBehavior = enum(u8) {
    /// Normal behavior; the block can be pushed and pulled.
    normal,
    /// The block is destroyed when pushed.
    destroy,
    /// The block cannot be pushed or pulled.
    block,
    /// The block is ignored by pistons.
    ignore,
    /// The block can only be pushed, not pulled.
    push_only,
};

/// Predefined AI goals that can be added to mob entities.
pub const BuiltinAiGoal = union(enum) {
    swim,
    wander_around: f32,
    melee_attack: f32,
    look_at_player: f32,
    look_around,
    escape_danger: f32,
    avoid_entity: f32,
    blaze_attack,
    creeper_ignite,
    eat_grass,
    zombie_attack: f32,
};

/// Node and terrain classification types evaluated during mob pathfinding.
pub const PathNodeType = enum(u8) {
    blocked,
    open,
    walkable,
    walkable_door,
    trapdoor,
    powder_snow,
    danger_powder_snow,
    fence,
    lava,
    water,
    water_border,
    rail,
    unpassable_rail,
    danger_fire,
    damage_fire,
    danger_other,
    damage_other,
    door_open,
    door_wood_closed,
    door_iron_closed,
    breach,
    leaves,
    sticky_honey,
    cocoa,
    damage_cautious,
    danger_trapdoor,
};

/// Minecraft 16 dye colors.
pub const DyeColor = enum(u8) {
    white,
    orange,
    magenta,
    light_blue,
    yellow,
    lime,
    pink,
    gray,
    light_gray,
    cyan,
    purple,
    blue,
    brown,
    green,
    red,
    black,
};

/// Villager profession identifiers.
pub const VillagerProfession = enum(u8) {
    none,
    armorer,
    butcher,
    cartographer,
    cleric,
    farmer,
    fisherman,
    fletcher,
    leatherworker,
    librarian,
    mason,
    nitwit,
    shepherd,
    toolsmith,
    weaponsmith,
};

/// Specialized data for sheep entities.
pub const SheepData = struct {
    color: DyeColor,
    is_sheared: bool,
};

/// Specialized data for wolf entities.
pub const WolfData = struct {
    is_tamed: bool,
    owner: ?Uuid,
    is_sitting: bool,
    collar_color: DyeColor,
    is_angry: bool,
    is_begging: bool,
};

/// Specialized data for cat entities.
pub const CatData = struct {
    is_tamed: bool,
    owner: ?Uuid,
    is_sitting: bool,
    collar_color: DyeColor,
};

/// Specialized data for villager entities.
pub const VillagerData = struct {
    profession: VillagerProfession,
    level: u8,
    experience: u32,
};

/// Specialized data for creeper entities.
pub const CreeperData = struct {
    is_powered: bool,
    fuse: i32,
    is_ignited: bool,
    explosion_radius: u8,
};

/// Specialized data for slime and magma cube entities.
pub const SlimeData = struct {
    size: i32,
};

/// Specialized data for enderman entities.
pub const EndermanData = struct {
    carried_block_state: ?u16,
    is_screaming: bool,
    is_staring: bool,
};

/// Specialized data for iron golem entities.
pub const IronGolemData = struct {
    is_player_created: bool,
};

/// Specialized data for fox entities.
pub const FoxData = struct {
    is_sitting: bool,
    is_sleeping: bool,
    is_crouching: bool,
};

/// Specialized data for ageable animal mobs.
pub const AgeableData = struct {
    is_baby: bool,
    age: i32,
    in_love_ticks: i32,
};

/// Specialized data for zombie entities.
pub const ZombieData = struct {
    is_baby: bool,
    can_break_doors: bool,
};

/// Defines how an explosion interacts with blocks in the world.
pub const ExplosionInteraction = enum(u8) {
    /// No interaction with blocks.
    none,
    /// Blocks are destroyed and may drop loot.
    block,
    /// Interaction typical for mob-caused explosions.
    mob,
    /// Interaction typical for TNT explosions.
    tnt,
    /// Triggers specific block behaviors.
    trigger,
};

/// Flags used to control block updates and notifications.
pub const BlockFlags = packed struct(u8) {
    /// Notifies neighbor blocks of the change.
    notify_neighbors: bool = false,
    /// Notifies client listeners of the change.
    notify_listeners: bool = false,
    /// Forces the block state even if it's the same.
    force_state: bool = false,
    /// Skips dropping items when the block is removed.
    skip_drops: bool = false,
    /// Indicates the block was moved (e.g., by a piston).
    moved: bool = false,
    /// Skips state replacement for redstone wires.
    skip_redstone_wire_state_replacement: bool = false,
    /// Skips the callback when a block entity is replaced.
    skip_block_entity_replaced_callback: bool = false,
    /// Skips the callback when a block is added.
    skip_block_added_callback: bool = false,
};

/// Flammability properties of a block.
pub const Flammable = struct {
    spread_chance: u8,
    burn_chance: u8,
};

/// Musical instruments used by note blocks.
pub const NoteblockInstrument = enum(u8) {
    harp,
    basedrum,
    snare,
    hat,
    bass,
    flute,
    bell,
    guitar,
    chime,
    xylophone,
    iron_xylophone,
    cow_bell,
    didgeridoo,
    bit,
    banjo,
    pling,
    trumpet,
    trumpet_exposed,
    trumpet_oxidized,
    trumpet_weathered,
    zombie,
    skeleton,
    creeper,
    dragon,
    wither_skeleton,
    piglin,
    custom_head,
};

/// Static definition of a Minecraft block type.
pub const Block = struct {
    /// The internal numerical ID of the block type.
    id: u16,
    /// The unique namespaced ID (e.g., "minecraft:stone").
    name: []const u8,
    /// How hard the block is to break (-1.0 for unbreakable blocks).
    hardness: f32,
    /// Resistance to explosions.
    blast_resistance: f32,
    /// The color of this block when displayed on a map.
    map_color: u8,
    /// The friction coefficient (0.6 default, 0.98 for ice).
    slipperiness: f32,
    /// How much this block affects entity walk speed.
    velocity_multiplier: f32,
    /// How much this block affects entity jump height.
    jump_velocity_multiplier: f32,
    /// The item ID associated with this block in inventory/drops.
    item_id: u16,
    /// The default block state ID when placed without extra properties.
    default_state_id: u16,
    /// All valid state IDs belonging to this block type.
    state_ids: []const u16,
    /// Whether this block is solid by default.
    is_solid: bool,
    /// Whether this block is considered air.
    is_air: bool,
    /// Whether this block is flammable.
    is_flammable: bool,
    /// Flammability details if flammable.
    flammable: ?Flammable,
};

/// A simple wrapper for a block state ID.
pub const BlockStateId = struct {
    id: u16,
};

pub const Entity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]entity";

    pub fn getId(self: Entity) u32 {
        return abi.call(raw.@"[method]entity.get-id", u32, .{abi.borrow(self)});
    }

    pub fn getUuid(self: Entity) Uuid {
        return abi.call(raw.@"[method]entity.get-uuid", Uuid, .{abi.borrow(self)});
    }

    pub fn getType(self: Entity) EntityType {
        return abi.call(raw.@"[method]entity.get-type", EntityType, .{abi.borrow(self)});
    }

    pub fn getPosition(self: Entity) Position {
        return abi.call(raw.@"[method]entity.get-position", Position, .{abi.borrow(self)});
    }

    pub fn getWorld(self: Entity) World {
        return abi.call(raw.@"[method]entity.get-world", World, .{abi.borrow(self)});
    }

    pub fn getYaw(self: Entity) f32 {
        return abi.call(raw.@"[method]entity.get-yaw", f32, .{abi.borrow(self)});
    }

    pub fn getPitch(self: Entity) f32 {
        return abi.call(raw.@"[method]entity.get-pitch", f32, .{abi.borrow(self)});
    }

    pub fn getHeadYaw(self: Entity) f32 {
        return abi.call(raw.@"[method]entity.get-head-yaw", f32, .{abi.borrow(self)});
    }

    /// Consumes `world_ref`.
    pub fn teleport(self: Entity, pos: Position, world_ref: World) void {
        return abi.call(raw.@"[method]entity.teleport", void, .{ abi.borrow(self), pos, world_ref });
    }

    pub fn setVelocity(self: Entity, velocity: Position) void {
        return abi.call(raw.@"[method]entity.set-velocity", void, .{ abi.borrow(self), velocity });
    }

    pub fn getVelocity(self: Entity) Position {
        return abi.call(raw.@"[method]entity.get-velocity", Position, .{abi.borrow(self)});
    }

    pub fn getPose(self: Entity) EntityPose {
        return abi.call(raw.@"[method]entity.get-pose", EntityPose, .{abi.borrow(self)});
    }

    pub fn getName(self: Entity) TextComponent {
        return abi.call(raw.@"[method]entity.get-name", TextComponent, .{abi.borrow(self)});
    }

    /// Consumes `name`.
    pub fn setCustomName(self: Entity, name: TextComponent) void {
        return abi.call(raw.@"[method]entity.set-custom-name", void, .{ abi.borrow(self), name });
    }

    pub fn getCustomName(self: Entity) ?TextComponent {
        return abi.call(raw.@"[method]entity.get-custom-name", ?TextComponent, .{abi.borrow(self)});
    }

    pub fn setCustomNameVisible(self: Entity, visible: bool) void {
        return abi.call(raw.@"[method]entity.set-custom-name-visible", void, .{ abi.borrow(self), visible });
    }

    pub fn isCustomNameVisible(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-custom-name-visible", bool, .{abi.borrow(self)});
    }

    pub fn isInvulnerable(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-invulnerable", bool, .{abi.borrow(self)});
    }

    pub fn setInvulnerable(self: Entity, invulnerable: bool) void {
        return abi.call(raw.@"[method]entity.set-invulnerable", void, .{ abi.borrow(self), invulnerable });
    }

    pub fn getFireTicks(self: Entity) i32 {
        return abi.call(raw.@"[method]entity.get-fire-ticks", i32, .{abi.borrow(self)});
    }

    pub fn setFireTicks(self: Entity, ticks: i32) void {
        return abi.call(raw.@"[method]entity.set-fire-ticks", void, .{ abi.borrow(self), ticks });
    }

    pub fn getFallDistance(self: Entity) f32 {
        return abi.call(raw.@"[method]entity.get-fall-distance", f32, .{abi.borrow(self)});
    }

    pub fn setFallDistance(self: Entity, distance: f32) void {
        return abi.call(raw.@"[method]entity.set-fall-distance", void, .{ abi.borrow(self), distance });
    }

    pub fn getTicksLived(self: Entity) i32 {
        return abi.call(raw.@"[method]entity.get-ticks-lived", i32, .{abi.borrow(self)});
    }

    pub fn setTicksLived(self: Entity, ticks: i32) void {
        return abi.call(raw.@"[method]entity.set-ticks-lived", void, .{ abi.borrow(self), ticks });
    }

    pub fn isSneaking(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-sneaking", bool, .{abi.borrow(self)});
    }

    pub fn setSneaking(self: Entity, sneaking: bool) void {
        return abi.call(raw.@"[method]entity.set-sneaking", void, .{ abi.borrow(self), sneaking });
    }

    pub fn isSprinting(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-sprinting", bool, .{abi.borrow(self)});
    }

    pub fn setSprinting(self: Entity, sprinting: bool) void {
        return abi.call(raw.@"[method]entity.set-sprinting", void, .{ abi.borrow(self), sprinting });
    }

    pub fn isSwimming(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-swimming", bool, .{abi.borrow(self)});
    }

    pub fn setSwimming(self: Entity, swimming: bool) void {
        return abi.call(raw.@"[method]entity.set-swimming", void, .{ abi.borrow(self), swimming });
    }

    pub fn isInvisible(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-invisible", bool, .{abi.borrow(self)});
    }

    pub fn setInvisible(self: Entity, invisible: bool) void {
        return abi.call(raw.@"[method]entity.set-invisible", void, .{ abi.borrow(self), invisible });
    }

    pub fn isGlowing(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-glowing", bool, .{abi.borrow(self)});
    }

    pub fn setGlowing(self: Entity, glowing: bool) void {
        return abi.call(raw.@"[method]entity.set-glowing", void, .{ abi.borrow(self), glowing });
    }

    pub fn isFallFlying(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-fall-flying", bool, .{abi.borrow(self)});
    }

    pub fn setFallFlying(self: Entity, fall_flying: bool) void {
        return abi.call(raw.@"[method]entity.set-fall-flying", void, .{ abi.borrow(self), fall_flying });
    }

    pub fn isSilent(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-silent", bool, .{abi.borrow(self)});
    }

    pub fn setSilent(self: Entity, silent: bool) void {
        return abi.call(raw.@"[method]entity.set-silent", void, .{ abi.borrow(self), silent });
    }

    pub fn hasGravity(self: Entity) bool {
        return abi.call(raw.@"[method]entity.has-gravity", bool, .{abi.borrow(self)});
    }

    pub fn setHasGravity(self: Entity, gravity: bool) void {
        return abi.call(raw.@"[method]entity.set-has-gravity", void, .{ abi.borrow(self), gravity });
    }

    pub fn getWidth(self: Entity) f32 {
        return abi.call(raw.@"[method]entity.get-width", f32, .{abi.borrow(self)});
    }

    pub fn getHeight(self: Entity) f32 {
        return abi.call(raw.@"[method]entity.get-height", f32, .{abi.borrow(self)});
    }

    pub fn setRotation(self: Entity, yaw: f32, pitch: f32) void {
        return abi.call(raw.@"[method]entity.set-rotation", void, .{ abi.borrow(self), yaw, pitch });
    }

    pub fn isOnGround(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-on-ground", bool, .{abi.borrow(self)});
    }

    pub fn isOnFire(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-on-fire", bool, .{abi.borrow(self)});
    }

    pub fn setOnFire(self: Entity, on_fire: bool) void {
        return abi.call(raw.@"[method]entity.set-on-fire", void, .{ abi.borrow(self), on_fire });
    }

    pub fn hasVisualFire(self: Entity) bool {
        return abi.call(raw.@"[method]entity.has-visual-fire", bool, .{abi.borrow(self)});
    }

    pub fn setVisualFire(self: Entity, visual_fire: bool) void {
        return abi.call(raw.@"[method]entity.set-visual-fire", void, .{ abi.borrow(self), visual_fire });
    }

    pub fn getPortalCooldown(self: Entity) u32 {
        return abi.call(raw.@"[method]entity.get-portal-cooldown", u32, .{abi.borrow(self)});
    }

    pub fn setPortalCooldown(self: Entity, cooldown: u32) void {
        return abi.call(raw.@"[method]entity.set-portal-cooldown", void, .{ abi.borrow(self), cooldown });
    }

    pub fn getRemainingAir(self: Entity) i32 {
        return abi.call(raw.@"[method]entity.get-remaining-air", i32, .{abi.borrow(self)});
    }

    pub fn setRemainingAir(self: Entity, air: i32) void {
        return abi.call(raw.@"[method]entity.set-remaining-air", void, .{ abi.borrow(self), air });
    }

    pub fn getMaxAir(self: Entity) i32 {
        return abi.call(raw.@"[method]entity.get-max-air", i32, .{abi.borrow(self)});
    }

    pub fn getEyeHeight(self: Entity) f32 {
        return abi.call(raw.@"[method]entity.get-eye-height", f32, .{abi.borrow(self)});
    }

    pub fn getEyePosition(self: Entity) Position {
        return abi.call(raw.@"[method]entity.get-eye-position", Position, .{abi.borrow(self)});
    }

    pub fn getNearbyEntities(self: Entity, x: f64, y: f64, z: f64) []const Entity {
        return abi.call(raw.@"[method]entity.get-nearby-entities", []const Entity, .{ abi.borrow(self), x, y, z });
    }

    pub fn getVehicle(self: Entity) ?Entity {
        return abi.call(raw.@"[method]entity.get-vehicle", ?Entity, .{abi.borrow(self)});
    }

    pub fn setVehicle(self: Entity, vehicle: ?Entity) void {
        return abi.call(raw.@"[method]entity.set-vehicle", void, .{ abi.borrow(self), vehicle });
    }

    pub fn getPassengers(self: Entity) []const Entity {
        return abi.call(raw.@"[method]entity.get-passengers", []const Entity, .{abi.borrow(self)});
    }

    /// Consumes `passenger`.
    pub fn addPassenger(self: Entity, passenger: Entity) void {
        return abi.call(raw.@"[method]entity.add-passenger", void, .{ abi.borrow(self), passenger });
    }

    /// Consumes `passenger`.
    pub fn removePassenger(self: Entity, passenger: Entity) void {
        return abi.call(raw.@"[method]entity.remove-passenger", void, .{ abi.borrow(self), passenger });
    }

    pub fn ejectPassengers(self: Entity) void {
        return abi.call(raw.@"[method]entity.eject-passengers", void, .{abi.borrow(self)});
    }

    pub fn getBoundingBox(self: Entity) BoundingBox {
        return abi.call(raw.@"[method]entity.get-bounding-box", BoundingBox, .{abi.borrow(self)});
    }

    pub fn isInWater(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-in-water", bool, .{abi.borrow(self)});
    }

    pub fn isInLava(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-in-lava", bool, .{abi.borrow(self)});
    }

    pub fn remove(self: Entity) void {
        return abi.call(raw.@"[method]entity.remove", void, .{abi.borrow(self)});
    }

    /// Performs a raycast from the entity's eye position in its looking direction.
    pub fn raycast(self: Entity, max_distance: f64, fluid_handling: bool) ?RaycastResult {
        return abi.call(raw.@"[method]entity.raycast", ?RaycastResult, .{ abi.borrow(self), max_distance, fluid_handling });
    }

    /// Performs a ray-trace from the entity's eye position in its looking direction to find the targeted block.
    pub fn rayTraceBlock(self: Entity, max_distance: f64, include_fluids: bool) ?RayTraceBlockResult {
        return abi.call(raw.@"[method]entity.ray-trace-block", ?RayTraceBlockResult, .{ abi.borrow(self), max_distance, include_fluids });
    }

    /// Performs a ray-trace from the entity's eye position in its looking direction to find the targeted entity.
    pub fn rayTraceEntity(self: Entity, max_distance: f64) ?RayTraceEntityResult {
        return abi.call(raw.@"[method]entity.ray-trace-entity", ?RayTraceEntityResult, .{ abi.borrow(self), max_distance });
    }

    /// Returns the entity currently targeted in the looking direction within the specified distance.
    pub fn getTargetEntity(self: Entity, max_distance: f64) ?Entity {
        return abi.call(raw.@"[method]entity.get-target-entity", ?Entity, .{ abi.borrow(self), max_distance });
    }

    /// Sets a namespaced custom data value on this entity.
    pub fn setCustomData(self: Entity, namespace: []const u8, key: []const u8, value: NbtTree) void {
        return abi.call(raw.@"[method]entity.set-custom-data", void, .{ abi.borrow(self), namespace, key, value });
    }

    /// Returns a namespaced custom data value from this entity, if set.
    pub fn getCustomData(self: Entity, namespace: []const u8, key: []const u8) ?NbtTree {
        return abi.call(raw.@"[method]entity.get-custom-data", ?NbtTree, .{ abi.borrow(self), namespace, key });
    }

    /// Removes a namespaced custom data value from this entity.
    pub fn removeCustomData(self: Entity, namespace: []const u8, key: []const u8) void {
        return abi.call(raw.@"[method]entity.remove-custom-data", void, .{ abi.borrow(self), namespace, key });
    }

    /// Returns whether this entity has a namespaced custom data value.
    pub fn hasCustomData(self: Entity, namespace: []const u8, key: []const u8) bool {
        return abi.call(raw.@"[method]entity.has-custom-data", bool, .{ abi.borrow(self), namespace, key });
    }

    /// Returns this entity as a living-entity if it has health and attributes, or none otherwise.
    pub fn asLiving(self: Entity) ?LivingEntity {
        return abi.call(raw.@"[method]entity.as-living", ?LivingEntity, .{abi.borrow(self)});
    }

    /// Returns this entity as a mob if it is an AI-driven mob entity, or none otherwise.
    pub fn asMob(self: Entity) ?Mob {
        return abi.call(raw.@"[method]entity.as-mob", ?Mob, .{abi.borrow(self)});
    }

    /// Returns whether this entity is a living entity.
    pub fn isLiving(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-living", bool, .{abi.borrow(self)});
    }

    /// Returns whether this entity is an AI-driven mob.
    pub fn isMob(self: Entity) bool {
        return abi.call(raw.@"[method]entity.is-mob", bool, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: Entity) void {
        abi.keep(Entity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: Entity) void {
        abi.drop(Entity, self);
    }
};

/// Represents a living entity with health, combat stats, attributes, and equipment (mobs, players, armor stands).
pub const LivingEntity = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]living-entity";

    /// Converts back to the base entity handle.
    pub fn asEntity(self: LivingEntity) Entity {
        return abi.call(raw.@"[method]living-entity.as-entity", Entity, .{abi.borrow(self)});
    }

    /// Returns this living entity as a mob if it has AI/navigation, or none otherwise.
    pub fn asMob(self: LivingEntity) ?Mob {
        return abi.call(raw.@"[method]living-entity.as-mob", ?Mob, .{abi.borrow(self)});
    }

    /// Returns whether this living entity is an AI-driven mob.
    pub fn isMob(self: LivingEntity) bool {
        return abi.call(raw.@"[method]living-entity.is-mob", bool, .{abi.borrow(self)});
    }

    /// Health & Combat
    pub fn getHealth(self: LivingEntity) f32 {
        return abi.call(raw.@"[method]living-entity.get-health", f32, .{abi.borrow(self)});
    }

    pub fn setHealth(self: LivingEntity, health: f32) void {
        return abi.call(raw.@"[method]living-entity.set-health", void, .{ abi.borrow(self), health });
    }

    pub fn getMaxHealth(self: LivingEntity) f32 {
        return abi.call(raw.@"[method]living-entity.get-max-health", f32, .{abi.borrow(self)});
    }

    pub fn setMaxHealth(self: LivingEntity, max_health: f32) void {
        return abi.call(raw.@"[method]living-entity.set-max-health", void, .{ abi.borrow(self), max_health });
    }

    pub fn damage(self: LivingEntity, amount: f32, damage_type: DamageType) void {
        return abi.call(raw.@"[method]living-entity.damage", void, .{ abi.borrow(self), amount, damage_type });
    }

    pub fn isDead(self: LivingEntity) bool {
        return abi.call(raw.@"[method]living-entity.is-dead", bool, .{abi.borrow(self)});
    }

    pub fn getAbsorption(self: LivingEntity) f32 {
        return abi.call(raw.@"[method]living-entity.get-absorption", f32, .{abi.borrow(self)});
    }

    pub fn setAbsorption(self: LivingEntity, amount: f32) void {
        return abi.call(raw.@"[method]living-entity.set-absorption", void, .{ abi.borrow(self), amount });
    }

    /// Attributes
    pub fn getAttributeValue(self: LivingEntity, attr: Attribute) f64 {
        return abi.call(raw.@"[method]living-entity.get-attribute-value", f64, .{ abi.borrow(self), attr });
    }

    pub fn getAttributeBase(self: LivingEntity, attr: Attribute) f64 {
        return abi.call(raw.@"[method]living-entity.get-attribute-base", f64, .{ abi.borrow(self), attr });
    }

    pub fn setAttributeBase(self: LivingEntity, attr: Attribute, value: f64) void {
        return abi.call(raw.@"[method]living-entity.set-attribute-base", void, .{ abi.borrow(self), attr, value });
    }

    pub fn addAttributeModifier(self: LivingEntity, attr: Attribute, modifier: AttributeModifier) void {
        return abi.call(raw.@"[method]living-entity.add-attribute-modifier", void, .{ abi.borrow(self), attr, modifier });
    }

    pub fn removeAttributeModifier(self: LivingEntity, attr: Attribute, id: []const u8) void {
        return abi.call(raw.@"[method]living-entity.remove-attribute-modifier", void, .{ abi.borrow(self), attr, id });
    }

    pub fn getAttributeModifiers(self: LivingEntity, attr: Attribute) []const AttributeModifier {
        return abi.call(raw.@"[method]living-entity.get-attribute-modifiers", []const AttributeModifier, .{ abi.borrow(self), attr });
    }

    pub fn resetAttribute(self: LivingEntity, attr: Attribute) void {
        return abi.call(raw.@"[method]living-entity.reset-attribute", void, .{ abi.borrow(self), attr });
    }

    pub fn resetAllAttributes(self: LivingEntity) void {
        return abi.call(raw.@"[method]living-entity.reset-all-attributes", void, .{abi.borrow(self)});
    }

    /// Equipment
    pub fn getEquipment(self: LivingEntity, slot: EquipmentSlot) ?ItemStack {
        return abi.call(raw.@"[method]living-entity.get-equipment", ?ItemStack, .{ abi.borrow(self), slot });
    }

    pub fn setEquipment(self: LivingEntity, slot: EquipmentSlot, stack: ?ItemStack) void {
        return abi.call(raw.@"[method]living-entity.set-equipment", void, .{ abi.borrow(self), slot, stack });
    }

    pub fn clearEquipment(self: LivingEntity) void {
        return abi.call(raw.@"[method]living-entity.clear-equipment", void, .{abi.borrow(self)});
    }

    pub fn getAge(self: LivingEntity) i32 {
        return abi.call(raw.@"[method]living-entity.get-age", i32, .{abi.borrow(self)});
    }

    pub fn setAge(self: LivingEntity, age: i32) void {
        return abi.call(raw.@"[method]living-entity.set-age", void, .{ abi.borrow(self), age });
    }

    /// Consumes `message`.
    pub fn sendSystemMessage(self: LivingEntity, message: TextComponent) void {
        return abi.call(raw.@"[method]living-entity.send-system-message", void, .{ abi.borrow(self), message });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: LivingEntity) void {
        abi.keep(LivingEntity, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: LivingEntity) void {
        abi.drop(LivingEntity, self);
    }
};

/// Represents an AI-driven mob entity with goals, targeting, and pathfinding navigation (e.g. zombies, villagers).
pub const Mob = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]mob";

    /// Converts back to the base entity handle.
    pub fn asEntity(self: Mob) Entity {
        return abi.call(raw.@"[method]mob.as-entity", Entity, .{abi.borrow(self)});
    }

    /// Converts to the living-entity handle.
    pub fn asLiving(self: Mob) LivingEntity {
        return abi.call(raw.@"[method]mob.as-living", LivingEntity, .{abi.borrow(self)});
    }

    /// Goals & Targeting
    pub fn addAiGoal(self: Mob, priority: u8, goal: BuiltinAiGoal) void {
        return abi.call(raw.@"[method]mob.add-ai-goal", void, .{ abi.borrow(self), priority, goal });
    }

    pub fn addCustomAiGoal(self: Mob, priority: u8, goal_id: u32) void {
        return abi.call(raw.@"[method]mob.add-custom-ai-goal", void, .{ abi.borrow(self), priority, goal_id });
    }

    pub fn clearAiGoals(self: Mob) void {
        return abi.call(raw.@"[method]mob.clear-ai-goals", void, .{abi.borrow(self)});
    }

    pub fn setAiDisabled(self: Mob, disabled: bool) void {
        return abi.call(raw.@"[method]mob.set-ai-disabled", void, .{ abi.borrow(self), disabled });
    }

    pub fn isAiDisabled(self: Mob) bool {
        return abi.call(raw.@"[method]mob.is-ai-disabled", bool, .{abi.borrow(self)});
    }

    pub fn setTarget(self: Mob, target: ?Entity) void {
        return abi.call(raw.@"[method]mob.set-target", void, .{ abi.borrow(self), target });
    }

    pub fn getTarget(self: Mob) ?Entity {
        return abi.call(raw.@"[method]mob.get-target", ?Entity, .{abi.borrow(self)});
    }

    /// Pathfinding & Navigation
    pub fn navigateToPos(self: Mob, pos: Position, speed: f64) bool {
        return abi.call(raw.@"[method]mob.navigate-to-pos", bool, .{ abi.borrow(self), pos, speed });
    }

    /// Consumes `target`.
    pub fn navigateToEntity(self: Mob, target: Entity, speed: f64) bool {
        return abi.call(raw.@"[method]mob.navigate-to-entity", bool, .{ abi.borrow(self), target, speed });
    }

    pub fn stopNavigation(self: Mob) void {
        return abi.call(raw.@"[method]mob.stop-navigation", void, .{abi.borrow(self)});
    }

    pub fn isNavigating(self: Mob) bool {
        return abi.call(raw.@"[method]mob.is-navigating", bool, .{abi.borrow(self)});
    }

    pub fn hasReachedDestination(self: Mob) bool {
        return abi.call(raw.@"[method]mob.has-reached-destination", bool, .{abi.borrow(self)});
    }

    pub fn setNavigationSpeed(self: Mob, speed: f64) void {
        return abi.call(raw.@"[method]mob.set-navigation-speed", void, .{ abi.borrow(self), speed });
    }

    pub fn canReach(self: Mob, pos: Position, max_distance: f32) bool {
        return abi.call(raw.@"[method]mob.can-reach", bool, .{ abi.borrow(self), pos, max_distance });
    }

    pub fn setPathfindingMalus(self: Mob, node_type: PathNodeType, malus: f32) void {
        return abi.call(raw.@"[method]mob.set-pathfinding-malus", void, .{ abi.borrow(self), node_type, malus });
    }

    pub fn getPathfindingMalus(self: Mob, node_type: PathNodeType) f32 {
        return abi.call(raw.@"[method]mob.get-pathfinding-malus", f32, .{ abi.borrow(self), node_type });
    }

    pub fn lookAt(self: Mob, pos: Position) void {
        return abi.call(raw.@"[method]mob.look-at", void, .{ abi.borrow(self), pos });
    }

    /// Consumes `target`.
    pub fn lookAtEntity(self: Mob, target: Entity) void {
        return abi.call(raw.@"[method]mob.look-at-entity", void, .{ abi.borrow(self), target });
    }

    /// Returns the specialized mob-specific data for this mob.
    pub fn getMobData(self: Mob) MobData {
        return abi.call(raw.@"[method]mob.get-mob-data", MobData, .{abi.borrow(self)});
    }

    /// Sets the specialized mob-specific data for this mob.
    /// Returns false if the provided data variant is incompatible with this mob.
    pub fn setMobData(self: Mob, data: MobData) bool {
        return abi.call(raw.@"[method]mob.set-mob-data", bool, .{ abi.borrow(self), data });
    }

    /// Sets the number of freeze ticks on this entity (0 to 140).
    pub fn setFreezeTicks(self: Mob, ticks: i32) void {
        return abi.call(raw.@"[method]mob.set-freeze-ticks", void, .{ abi.borrow(self), ticks });
    }

    /// Returns the number of freeze ticks currently on this entity.
    pub fn getFreezeTicks(self: Mob) i32 {
        return abi.call(raw.@"[method]mob.get-freeze-ticks", i32, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: Mob) void {
        abi.keep(Mob, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: Mob) void {
        abi.drop(Mob, self);
    }
};

/// Result of an entity ray-trace operation.
pub const RayTraceEntityResult = struct {
    /// The entity that was hit.
    entity: Entity,
    /// The exact hit coordinates in world space.
    hit_pos: Position,
    /// The distance from the ray start to the hit position.
    distance: f64,
};

pub const Chunk = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]chunk";

    /// Gets the chunk's X coordinate.
    pub fn getX(self: Chunk) i32 {
        return abi.call(raw.@"[method]chunk.get-x", i32, .{abi.borrow(self)});
    }

    /// Gets the chunk's Z coordinate.
    pub fn getZ(self: Chunk) i32 {
        return abi.call(raw.@"[method]chunk.get-z", i32, .{abi.borrow(self)});
    }

    /// Gets the block state ID at the specified position relative to the chunk.
    /// X and Z must be in the range [0, 15].
    pub fn getBlockStateId(self: Chunk, pos: BlockPos) u16 {
        return abi.call(raw.@"[method]chunk.get-block-state-id", u16, .{ abi.borrow(self), pos });
    }

    /// Gets detailed block state information at the specified position relative to the chunk.
    /// X and Z must be in the range [0, 15].
    pub fn getBlockState(self: Chunk, pos: BlockPos) BlockState {
        return abi.call(raw.@"[method]chunk.get-block-state", BlockState, .{ abi.borrow(self), pos });
    }

    /// Sets the block state at the specified position relative to the chunk.
    /// X and Z must be in the range [0, 15].
    pub fn setBlockState(self: Chunk, pos: BlockPos, state: u16) void {
        return abi.call(raw.@"[method]chunk.set-block-state", void, .{ abi.borrow(self), pos, state });
    }

    /// Gets the block at the specified position relative to the chunk.
    /// X and Z must be in the range [0, 15].
    pub fn getBlock(self: Chunk, pos: BlockPos) Block {
        return abi.call(raw.@"[method]chunk.get-block", Block, .{ abi.borrow(self), pos });
    }

    /// Sets the block at the specified position relative to the chunk using its default state.
    /// X and Z must be in the range [0, 15].
    pub fn setBlock(self: Chunk, pos: BlockPos, block: Block) void {
        return abi.call(raw.@"[method]chunk.set-block", void, .{ abi.borrow(self), pos, block });
    }

    /// Sets the block by block ID at the specified position relative to the chunk using its default state.
    /// X and Z must be in the range [0, 15].
    pub fn setBlockById(self: Chunk, pos: BlockPos, block_id: u16) void {
        return abi.call(raw.@"[method]chunk.set-block-by-id", void, .{ abi.borrow(self), pos, block_id });
    }

    /// Gets the biome at the specified position relative to the chunk.
    /// X and Z must be in the range [0, 15].
    pub fn getBiome(self: Chunk, pos: BlockPos) Biome {
        return abi.call(raw.@"[method]chunk.get-biome", Biome, .{ abi.borrow(self), pos });
    }

    /// Gets the block entity at the specified position relative to the chunk, if any.
    /// X and Z must be in the range [0, 15].
    pub fn getBlockEntity(self: Chunk, pos: BlockPos) ?BlockEntityType {
        return abi.call(raw.@"[method]chunk.get-block-entity", ?BlockEntityType, .{ abi.borrow(self), pos });
    }

    /// Gets the highest non-air block Y coordinate at the specified X and Z relative to the chunk.
    /// X and Z must be in the range [0, 15].
    pub fn getTopBlockY(self: Chunk, x: i32, z: i32) i32 {
        return abi.call(raw.@"[method]chunk.get-top-block-y", i32, .{ abi.borrow(self), x, z });
    }

    /// Gets the sky light level at the specified position relative to the chunk.
    /// X and Z must be in the range [0, 15].
    pub fn getSkyLight(self: Chunk, pos: BlockPos) u8 {
        return abi.call(raw.@"[method]chunk.get-sky-light", u8, .{ abi.borrow(self), pos });
    }

    /// Gets the block light level at the specified position relative to the chunk.
    /// X and Z must be in the range [0, 15].
    pub fn getBlockLight(self: Chunk, pos: BlockPos) u8 {
        return abi.call(raw.@"[method]chunk.get-block-light", u8, .{ abi.borrow(self), pos });
    }

    /// Sets a namespaced custom data value on this chunk.
    pub fn setCustomData(self: Chunk, namespace: []const u8, key: []const u8, value: NbtTree) void {
        return abi.call(raw.@"[method]chunk.set-custom-data", void, .{ abi.borrow(self), namespace, key, value });
    }

    /// Returns a namespaced custom data value from this chunk, if set.
    pub fn getCustomData(self: Chunk, namespace: []const u8, key: []const u8) ?NbtTree {
        return abi.call(raw.@"[method]chunk.get-custom-data", ?NbtTree, .{ abi.borrow(self), namespace, key });
    }

    /// Removes a namespaced custom data value from this chunk.
    pub fn removeCustomData(self: Chunk, namespace: []const u8, key: []const u8) void {
        return abi.call(raw.@"[method]chunk.remove-custom-data", void, .{ abi.borrow(self), namespace, key });
    }

    /// Returns whether this chunk has a namespaced custom data value.
    pub fn hasCustomData(self: Chunk, namespace: []const u8, key: []const u8) bool {
        return abi.call(raw.@"[method]chunk.has-custom-data", bool, .{ abi.borrow(self), namespace, key });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: Chunk) void {
        abi.keep(Chunk, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: Chunk) void {
        abi.drop(Chunk, self);
    }
};

/// Represents the boundaries of a world.
pub const WorldBorder = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]world-border";

    /// Gets the center X coordinate of the border.
    pub fn getCenterX(self: WorldBorder) f64 {
        return abi.call(raw.@"[method]world-border.get-center-x", f64, .{abi.borrow(self)});
    }

    /// Gets the center Z coordinate of the border.
    pub fn getCenterZ(self: WorldBorder) f64 {
        return abi.call(raw.@"[method]world-border.get-center-z", f64, .{abi.borrow(self)});
    }

    /// Gets the center position of the border.
    pub fn getCenter(self: WorldBorder) Position {
        return abi.call(raw.@"[method]world-border.get-center", Position, .{abi.borrow(self)});
    }

    /// Sets the center coordinates of the border.
    pub fn setCenter(self: WorldBorder, x: f64, z: f64) void {
        return abi.call(raw.@"[method]world-border.set-center", void, .{ abi.borrow(self), x, z });
    }

    /// Gets the current diameter of the border.
    pub fn getDiameter(self: WorldBorder) f64 {
        return abi.call(raw.@"[method]world-border.get-diameter", f64, .{abi.borrow(self)});
    }

    /// Gets the current diameter of the border (alias for get-diameter).
    pub fn getSize(self: WorldBorder) f64 {
        return abi.call(raw.@"[method]world-border.get-size", f64, .{abi.borrow(self)});
    }

    /// Sets the diameter of the border, optionally with a speed (in ticks) for lerping.
    pub fn setDiameter(self: WorldBorder, diameter: f64, speed: ?u64) void {
        return abi.call(raw.@"[method]world-border.set-diameter", void, .{ abi.borrow(self), diameter, speed });
    }

    /// Sets the size of the border instantly.
    pub fn setSize(self: WorldBorder, size: f64) void {
        return abi.call(raw.@"[method]world-border.set-size", void, .{ abi.borrow(self), size });
    }

    /// Sets the size of the border with a transition duration in seconds.
    pub fn setSizeTransition(self: WorldBorder, new_size: f64, time_seconds: u64) void {
        return abi.call(raw.@"[method]world-border.set-size-transition", void, .{ abi.borrow(self), new_size, time_seconds });
    }

    /// Gets the target diameter when lerping/transitioning.
    pub fn getTargetDiameter(self: WorldBorder) f64 {
        return abi.call(raw.@"[method]world-border.get-target-diameter", f64, .{abi.borrow(self)});
    }

    /// Gets the target transition duration/speed in ticks.
    pub fn getTargetSpeed(self: WorldBorder) i64 {
        return abi.call(raw.@"[method]world-border.get-target-speed", i64, .{abi.borrow(self)});
    }

    /// Gets the distance from the border where warning effects start (in blocks).
    pub fn getWarningDistance(self: WorldBorder) i32 {
        return abi.call(raw.@"[method]world-border.get-warning-distance", i32, .{abi.borrow(self)});
    }

    /// Sets the warning distance (in blocks).
    pub fn setWarningDistance(self: WorldBorder, distance: i32) void {
        return abi.call(raw.@"[method]world-border.set-warning-distance", void, .{ abi.borrow(self), distance });
    }

    /// Gets the time before a player hitting the border is warned (in seconds).
    pub fn getWarningDelay(self: WorldBorder) i32 {
        return abi.call(raw.@"[method]world-border.get-warning-delay", i32, .{abi.borrow(self)});
    }

    /// Sets the warning delay (in seconds).
    pub fn setWarningDelay(self: WorldBorder, delay: i32) void {
        return abi.call(raw.@"[method]world-border.set-warning-delay", void, .{ abi.borrow(self), delay });
    }

    /// Gets the warning time (alias for get-warning-delay).
    pub fn getWarningTime(self: WorldBorder) i32 {
        return abi.call(raw.@"[method]world-border.get-warning-time", i32, .{abi.borrow(self)});
    }

    /// Sets the warning time (alias for set-warning-delay).
    pub fn setWarningTime(self: WorldBorder, time: i32) void {
        return abi.call(raw.@"[method]world-border.set-warning-time", void, .{ abi.borrow(self), time });
    }

    /// Gets the safe damage buffer distance outside the border (in blocks).
    pub fn getDamageBuffer(self: WorldBorder) f64 {
        return abi.call(raw.@"[method]world-border.get-damage-buffer", f64, .{abi.borrow(self)});
    }

    /// Sets the safe damage buffer distance outside the border (in blocks).
    pub fn setDamageBuffer(self: WorldBorder, buffer: f64) void {
        return abi.call(raw.@"[method]world-border.set-damage-buffer", void, .{ abi.borrow(self), buffer });
    }

    /// Gets the damage amount inflicted per block per second when outside the buffer.
    pub fn getDamageAmount(self: WorldBorder) f64 {
        return abi.call(raw.@"[method]world-border.get-damage-amount", f64, .{abi.borrow(self)});
    }

    /// Sets the damage amount inflicted per block per second when outside the buffer.
    pub fn setDamageAmount(self: WorldBorder, damage: f64) void {
        return abi.call(raw.@"[method]world-border.set-damage-amount", void, .{ abi.borrow(self), damage });
    }

    /// Returns whether the specified coordinates are within the border.
    pub fn contains(self: WorldBorder, x: f64, z: f64) bool {
        return abi.call(raw.@"[method]world-border.contains", bool, .{ abi.borrow(self), x, z });
    }

    /// Returns whether the specified position is within the border.
    pub fn containsPos(self: WorldBorder, pos: Position) bool {
        return abi.call(raw.@"[method]world-border.contains-pos", bool, .{ abi.borrow(self), pos });
    }

    /// Resets the world border to vanilla default settings.
    pub fn reset(self: WorldBorder) void {
        return abi.call(raw.@"[method]world-border.reset", void, .{abi.borrow(self)});
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: WorldBorder) void {
        abi.keep(WorldBorder, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: WorldBorder) void {
        abi.drop(WorldBorder, self);
    }
};

/// Represents an axis-aligned bounding box.
pub const BoundingBox = struct {
    min: Position,
    max: Position,
};

/// Detailed information about a block's state.
pub const BlockState = struct {
    /// The internal numerical ID of the block state.
    id: u16,
    /// The numerical ID of the parent block type.
    block_id: u16,
    /// The namespaced identifier of the parent block type.
    block_name: []const u8,
    /// The light level emitted by this block (0-15).
    luminance: u8,
    /// How much light this block blocks.
    opacity: u8,
    /// The hardness of the block, determining mining speed.
    hardness: f32,
    /// Whether this block is considered air.
    is_air: bool,
    /// Whether this block is a liquid.
    is_liquid: bool,
    /// Whether this block is a solid.
    is_solid: bool,
    /// Whether this block occupies a full cube.
    is_full_cube: bool,
    /// How this block behaves when pushed by a piston.
    piston_behavior: PistonBehavior,
    /// Whether this block receives random ticks.
    has_random_ticks: bool,
    /// Whether this block can catch fire.
    burnable: bool,
    /// Whether a specific tool is required to mine this block and get drops.
    tool_required: bool,
    /// Whether this block has transparency on its sides.
    sided_transparency: bool,
    /// Whether this block can be replaced by another block (e.g., grass).
    replaceable: bool,
    /// Whether this block is considered a solid block for redstone purposes.
    is_solid_block: bool,
    /// The numerical ID of the block entity type, if any.
    block_entity_type: u16,
    /// The instrument used for note blocks when placed on top of this block.
    instrument: NoteblockInstrument,
    /// The collision shapes of this block.
    collision_shapes: []const BoundingBox,
    /// The outline shapes of this block.
    outline_shapes: []const BoundingBox,
    /// Whether the bottom side of the block is solid.
    down_side_solid: bool,
    /// Whether the top side of the block is solid.
    up_side_solid: bool,
    /// Whether the north side of the block is solid.
    north_side_solid: bool,
    /// Whether the south side of the block is solid.
    south_side_solid: bool,
    /// Whether the west side of the block is solid.
    west_side_solid: bool,
    /// Whether the east side of the block is solid.
    east_side_solid: bool,
    /// Whether the center of the bottom side is solid.
    down_center_solid: bool,
    /// Whether the center of the top side is solid.
    up_center_solid: bool,
    /// The color of this block when displayed on a map.
    map_color: u8,
    /// The block state's property key-value pairs (e.g. facing=north).
    properties: []const struct { []const u8, []const u8 },
};

/// Represents the configured shared spawn location for a world.
pub const WorldSpawnLocation = struct {
    /// The block position of the shared world spawn.
    pos: BlockPos,
    /// The yaw players should face when spawning here.
    yaw: f32,
    /// The pitch players should use when spawning here.
    pitch: f32,
};

/// Represents a cardinal direction or block face.
pub const BlockDirection = enum(u8) {
    down,
    up,
    north,
    south,
    west,
    east,
};

/// Specialized data for shulker entities.
pub const ShulkerData = struct {
    attached_face: BlockDirection,
    peek_amount: u8,
    color: ?DyeColor,
};

/// Tagged variant holding specialized data for specific mob types.
pub const MobData = union(enum) {
    sheep: SheepData,
    wolf: WolfData,
    cat: CatData,
    villager: VillagerData,
    creeper: CreeperData,
    slime: SlimeData,
    enderman: EndermanData,
    iron_golem: IronGolemData,
    fox: FoxData,
    ageable: AgeableData,
    zombie: ZombieData,
    shulker: ShulkerData,
    generic,
};

/// Result of a raycast operation.
pub const RaycastResult = struct {
    /// The block position that was hit.
    pos: BlockPos,
    /// The face of the block that was hit.
    face: BlockDirection,
};

/// Detailed result of a block ray-trace operation.
pub const RayTraceBlockResult = struct {
    /// The block position that was hit.
    pos: BlockPos,
    /// The face of the block that was hit.
    face: BlockDirection,
    /// The exact hit coordinates in world space.
    hit_pos: Position,
};

/// Represents a Minecraft world (dimension).
pub const World = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]world";

    /// Returns the unique identifier of this world.
    pub fn getId(self: World) []const u8 {
        return abi.call(raw.@"[method]world.get-id", []const u8, .{abi.borrow(self)});
    }

    /// Gets the world border associated with this world (alias for get-world-border).
    pub fn getBorder(self: World) WorldBorder {
        return abi.call(raw.@"[method]world.get-border", WorldBorder, .{abi.borrow(self)});
    }

    /// Gets the world border associated with this world.
    pub fn getWorldBorder(self: World) WorldBorder {
        return abi.call(raw.@"[method]world.get-world-border", WorldBorder, .{abi.borrow(self)});
    }

    /// Gets the chunk at the specified chunk coordinates.
    pub fn getChunk(self: World, x: i32, z: i32) ?Chunk {
        return abi.call(raw.@"[method]world.get-chunk", ?Chunk, .{ abi.borrow(self), x, z });
    }

    /// Gets the block state ID at the specified position.
    pub fn getBlockStateId(self: World, pos: BlockPos) u16 {
        return abi.call(raw.@"[method]world.get-block-state-id", u16, .{ abi.borrow(self), pos });
    }

    /// Gets detailed block state information at the specified position.
    pub fn getBlockState(self: World, pos: BlockPos) BlockState {
        return abi.call(raw.@"[method]world.get-block-state", BlockState, .{ abi.borrow(self), pos });
    }

    /// Sets the block state at the specified position.
    pub fn setBlockState(self: World, pos: BlockPos, state: u16, update_flags: BlockFlags) void {
        return abi.call(raw.@"[method]world.set-block-state", void, .{ abi.borrow(self), pos, state, update_flags });
    }

    /// Gets the block type at the specified position.
    pub fn getBlock(self: World, pos: BlockPos) Block {
        return abi.call(raw.@"[method]world.get-block", Block, .{ abi.borrow(self), pos });
    }

    /// Gets the numerical block ID at the specified position.
    pub fn getBlockId(self: World, pos: BlockPos) u16 {
        return abi.call(raw.@"[method]world.get-block-id", u16, .{ abi.borrow(self), pos });
    }

    /// Sets the block at the specified position using its default state.
    pub fn setBlock(self: World, pos: BlockPos, block: Block, update_flags: BlockFlags) void {
        return abi.call(raw.@"[method]world.set-block", void, .{ abi.borrow(self), pos, block, update_flags });
    }

    /// Sets the block at the specified position by numerical block ID using its default state.
    pub fn setBlockById(self: World, pos: BlockPos, block_id: u16, update_flags: BlockFlags) void {
        return abi.call(raw.@"[method]world.set-block-by-id", void, .{ abi.borrow(self), pos, block_id, update_flags });
    }

    /// Sets the block at the specified position by namespaced name using its default state.
    /// Returns true if the block was recognized and set.
    pub fn setBlockByName(self: World, pos: BlockPos, name: []const u8, update_flags: BlockFlags) bool {
        return abi.call(raw.@"[method]world.set-block-by-name", bool, .{ abi.borrow(self), pos, name, update_flags });
    }

    /// Returns the current time of day in ticks.
    pub fn getTimeOfDay(self: World) u64 {
        return abi.call(raw.@"[method]world.get-time-of-day", u64, .{abi.borrow(self)});
    }

    /// Sets the current time of day in ticks.
    pub fn setTimeOfDay(self: World, time: u64) void {
        return abi.call(raw.@"[method]world.set-time-of-day", void, .{ abi.borrow(self), time });
    }

    /// Returns the total age of the world in ticks.
    pub fn getWorldAge(self: World) u64 {
        return abi.call(raw.@"[method]world.get-world-age", u64, .{abi.borrow(self)});
    }

    /// Returns the dimension type (e.g., \"minecraft:overworld\").
    pub fn getDimension(self: World) []const u8 {
        return abi.call(raw.@"[method]world.get-dimension", []const u8, .{abi.borrow(self)});
    }

    /// Returns the configured shared spawn location for this world.
    pub fn getSpawnLocation(self: World) WorldSpawnLocation {
        return abi.call(raw.@"[method]world.get-spawn-location", WorldSpawnLocation, .{abi.borrow(self)});
    }

    /// Gets the highest non-air block Y coordinate at the specified X and Z.
    pub fn getTopBlockY(self: World, x: i32, z: i32) i32 {
        return abi.call(raw.@"[method]world.get-top-block-y", i32, .{ abi.borrow(self), x, z });
    }

    /// Gets the highest motion-blocking block Y coordinate.
    pub fn getMotionBlockingHeight(self: World, x: i32, z: i32) i32 {
        return abi.call(raw.@"[method]world.get-motion-blocking-height", i32, .{ abi.borrow(self), x, z });
    }

    /// Returns whether it is currently raining or snowing.
    pub fn isRaining(self: World) bool {
        return abi.call(raw.@"[method]world.is-raining", bool, .{abi.borrow(self)});
    }

    /// Sets whether it should be raining or snowing.
    pub fn setRaining(self: World, raining: bool) void {
        return abi.call(raw.@"[method]world.set-raining", void, .{ abi.borrow(self), raining });
    }

    /// Returns whether there is currently a thunderstorm.
    pub fn isThundering(self: World) bool {
        return abi.call(raw.@"[method]world.is-thundering", bool, .{abi.borrow(self)});
    }

    /// Sets whether there should be a thunderstorm.
    pub fn setThundering(self: World, thundering: bool) void {
        return abi.call(raw.@"[method]world.set-thundering", void, .{ abi.borrow(self), thundering });
    }

    /// Broadcasts a system message to all players in this world.
    ///
    /// Consumes `message`.
    pub fn broadcastSystemMessage(self: World, message: TextComponent, overlay: bool) void {
        return abi.call(raw.@"[method]world.broadcast-system-message", void, .{ abi.borrow(self), message, overlay });
    }

    /// Returns the scoreboard associated with this world.
    pub fn getScoreboard(self: World) Scoreboard {
        return abi.call(raw.@"[method]world.get-scoreboard", Scoreboard, .{abi.borrow(self)});
    }

    /// Plays a sound at the specified position for all players in the world.
    pub fn playSound(self: World, sound: Sound, category: SoundCategory, pos: Position, volume: f32, pitch: f32) void {
        return abi.call(raw.@"[method]world.play-sound", void, .{ abi.borrow(self), sound, category, pos, volume, pitch });
    }

    /// Plays a custom resource pack sound identifier at the specified position for all players in the world.
    pub fn playCustomSound(self: World, sound_name: []const u8, category: SoundCategory, pos: Position, volume: f32, pitch: f32) void {
        return abi.call(raw.@"[method]world.play-custom-sound", void, .{ abi.borrow(self), sound_name, category, pos, volume, pitch });
    }

    /// Spawns particles at the specified position.
    pub fn spawnParticle(self: World, particle: Particle, pos: Position, offset: Position, max_speed: f32, count: i32) void {
        return abi.call(raw.@"[method]world.spawn-particle", void, .{ abi.borrow(self), particle, pos, offset, max_speed, count });
    }

    /// Creates an explosion at the specified position.
    pub fn createExplosion(self: World, pos: Position, power: f32, create_fire: bool, interaction: ExplosionInteraction) void {
        return abi.call(raw.@"[method]world.create-explosion", void, .{ abi.borrow(self), pos, power, create_fire, interaction });
    }

    /// Returns the sea level of this world.
    pub fn getSeaLevel(self: World) i32 {
        return abi.call(raw.@"[method]world.get-sea-level", i32, .{abi.borrow(self)});
    }

    /// Returns the minimum Y coordinate (bottom of the world).
    pub fn getMinY(self: World) i32 {
        return abi.call(raw.@"[method]world.get-min-y", i32, .{abi.borrow(self)});
    }

    /// Returns the sky light level at the specified position.
    pub fn getSkyLight(self: World, pos: BlockPos) u8 {
        return abi.call(raw.@"[method]world.get-sky-light", u8, .{ abi.borrow(self), pos });
    }

    /// Sets the sky light level at the specified position.
    pub fn setSkyLight(self: World, pos: BlockPos, level: u8) void {
        return abi.call(raw.@"[method]world.set-sky-light", void, .{ abi.borrow(self), pos, level });
    }

    /// Returns the block light level at the specified position.
    pub fn getBlockLight(self: World, pos: BlockPos) u8 {
        return abi.call(raw.@"[method]world.get-block-light", u8, .{ abi.borrow(self), pos });
    }

    /// Sets the block light level at the specified position.
    pub fn setBlockLight(self: World, pos: BlockPos, level: u8) void {
        return abi.call(raw.@"[method]world.set-block-light", void, .{ abi.borrow(self), pos, level });
    }

    /// Returns the biome at the specified position.
    pub fn getBiome(self: World, pos: BlockPos) Biome {
        return abi.call(raw.@"[method]world.get-biome", Biome, .{ abi.borrow(self), pos });
    }

    /// Spawns an entity of the specified type at the given position.
    pub fn spawnEntity(self: World, entity_type: EntityType, pos: Position) Entity {
        return abi.call(raw.@"[method]world.spawn-entity", Entity, .{ abi.borrow(self), entity_type, pos });
    }

    /// Returns a list of all entities in this world.
    pub fn getEntities(self: World) []const Entity {
        return abi.call(raw.@"[method]world.get-entities", []const Entity, .{abi.borrow(self)});
    }

    /// Spawns a lightning bolt at the specified position.
    pub fn strikeLightning(self: World, pos: Position, effect_only: bool) void {
        return abi.call(raw.@"[method]world.strike-lightning", void, .{ abi.borrow(self), pos, effect_only });
    }

    /// Performs a raycast from start position to end position to hit blocks.
    pub fn rayTraceBlocks(self: World, start: Position, end: Position) ?Position {
        return abi.call(raw.@"[method]world.ray-trace-blocks", ?Position, .{ abi.borrow(self), start, end });
    }

    /// Performs a raycast from start position to end position to hit blocks, with optional fluid handling.
    pub fn rayTraceBlock(self: World, start: Position, end: Position, include_fluids: bool) ?RayTraceBlockResult {
        return abi.call(raw.@"[method]world.ray-trace-block", ?RayTraceBlockResult, .{ abi.borrow(self), start, end, include_fluids });
    }

    /// Performs a raycast from start position to end position to find the closest entity hit.
    pub fn rayTraceEntity(self: World, start: Position, end: Position) ?RayTraceEntityResult {
        return abi.call(raw.@"[method]world.ray-trace-entity", ?RayTraceEntityResult, .{ abi.borrow(self), start, end });
    }

    /// Performs a raycast from start position to end position to find all entities hit along the ray.
    pub fn rayTraceEntities(self: World, start: Position, end: Position) []const RayTraceEntityResult {
        return abi.call(raw.@"[method]world.ray-trace-entities", []const RayTraceEntityResult, .{ abi.borrow(self), start, end });
    }

    /// Returns the block entity at the specified position, if any.
    pub fn getBlockEntity(self: World, pos: BlockPos) ?BlockEntityType {
        return abi.call(raw.@"[method]world.get-block-entity", ?BlockEntityType, .{ abi.borrow(self), pos });
    }

    /// Returns the raw NBT bytes of the block entity at the given position, if any.
    pub fn getBlockEntityNbt(self: World, pos: BlockPos) ?[]const u8 {
        return abi.call(raw.@"[method]world.get-block-entity-nbt", ?[]const u8, .{ abi.borrow(self), pos });
    }

    /// Restores a block entity from raw NBT bytes at the given position.
    /// The NBT must contain a valid "id" field (e.g. "minecraft:chest").
    pub fn setBlockEntityNbt(self: World, pos: BlockPos, nbt_data: []const u8) abi.Result(void, []const u8) {
        return abi.call(raw.@"[method]world.set-block-entity-nbt", abi.Result(void, []const u8), .{ abi.borrow(self), pos, nbt_data });
    }

    /// Returns the name of this world (e.g. "world", "world_nether", "arena_1").
    pub fn getName(self: World) []const u8 {
        return abi.call(raw.@"[method]world.get-name", []const u8, .{abi.borrow(self)});
    }

    /// Saves all chunk data, block entities, and entities for this world to disk.
    pub fn save(self: World) abi.Result(void, []const u8) {
        return abi.call(raw.@"[method]world.save", abi.Result(void, []const u8), .{abi.borrow(self)});
    }

    /// Sets a custom chunk generator for this world using a registered generator ID.
    pub fn setChunkGenerator(self: World, generator_id: u32) void {
        return abi.call(raw.@"[method]world.set-chunk-generator", void, .{ abi.borrow(self), generator_id });
    }

    /// Sets a namespaced custom data value on this world.
    pub fn setCustomData(self: World, namespace: []const u8, key: []const u8, value: NbtTree) void {
        return abi.call(raw.@"[method]world.set-custom-data", void, .{ abi.borrow(self), namespace, key, value });
    }

    /// Returns a namespaced custom data value from this world, if set.
    pub fn getCustomData(self: World, namespace: []const u8, key: []const u8) ?NbtTree {
        return abi.call(raw.@"[method]world.get-custom-data", ?NbtTree, .{ abi.borrow(self), namespace, key });
    }

    /// Removes a namespaced custom data value from this world.
    pub fn removeCustomData(self: World, namespace: []const u8, key: []const u8) void {
        return abi.call(raw.@"[method]world.remove-custom-data", void, .{ abi.borrow(self), namespace, key });
    }

    /// Returns whether this world has a namespaced custom data value.
    pub fn hasCustomData(self: World, namespace: []const u8, key: []const u8) bool {
        return abi.call(raw.@"[method]world.has-custom-data", bool, .{ abi.borrow(self), namespace, key });
    }

    /// Gets the value of a game rule in this world.
    pub fn getGameRule(self: World, rule: GameRule) GameRuleValue {
        return abi.call(raw.@"[method]world.get-game-rule", GameRuleValue, .{ abi.borrow(self), rule });
    }

    /// Sets the value of a game rule in this world.
    pub fn setGameRule(self: World, rule: GameRule, value: GameRuleValue) void {
        return abi.call(raw.@"[method]world.set-game-rule", void, .{ abi.borrow(self), rule, value });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: World) void {
        abi.keep(World, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: World) void {
        abi.drop(World, self);
    }
};

/// Generation phase for custom chunk generation.
pub const GenerationPhase = enum(u8) {
    biomes,
    noise,
    surface,
    features,
};

/// Mutable chunk buffer representing a 16x16 column during custom world generation.
pub const ChunkBuffer = enum(u32) {
    _,

    pub const resource_drop = &raw.@"[resource-drop]chunk-buffer";

    /// Returns the chunk X coordinate.
    pub fn getX(self: ChunkBuffer) i32 {
        return abi.call(raw.@"[method]chunk-buffer.get-x", i32, .{abi.borrow(self)});
    }

    /// Returns the chunk Z coordinate.
    pub fn getZ(self: ChunkBuffer) i32 {
        return abi.call(raw.@"[method]chunk-buffer.get-z", i32, .{abi.borrow(self)});
    }

    /// Returns the minimum Y coordinate for this world.
    pub fn getMinY(self: ChunkBuffer) i32 {
        return abi.call(raw.@"[method]chunk-buffer.get-min-y", i32, .{abi.borrow(self)});
    }

    /// Returns the height of the chunk in blocks.
    pub fn getHeight(self: ChunkBuffer) u32 {
        return abi.call(raw.@"[method]chunk-buffer.get-height", u32, .{abi.borrow(self)});
    }

    /// Sets the block state ID at local coordinates (0..16, y, 0..16).
    pub fn setBlockStateId(self: ChunkBuffer, x: u8, y: i32, z: u8, state_id: u16) void {
        return abi.call(raw.@"[method]chunk-buffer.set-block-state-id", void, .{ abi.borrow(self), x, y, z, state_id });
    }

    /// Gets the block state ID at local coordinates (0..16, y, 0..16).
    pub fn getBlockStateId(self: ChunkBuffer, x: u8, y: i32, z: u8) u16 {
        return abi.call(raw.@"[method]chunk-buffer.get-block-state-id", u16, .{ abi.borrow(self), x, y, z });
    }

    /// Fills an entire horizontal 16x16 layer at the given Y level with a block state ID.
    pub fn fillLayer(self: ChunkBuffer, y: i32, state_id: u16) void {
        return abi.call(raw.@"[method]chunk-buffer.fill-layer", void, .{ abi.borrow(self), y, state_id });
    }

    /// Fills a vertical column from min-y to max-y at local (x, z) with a block state ID.
    pub fn fillRange(self: ChunkBuffer, x: u8, min_y: i32, max_y: i32, z: u8, state_id: u16) void {
        return abi.call(raw.@"[method]chunk-buffer.fill-range", void, .{ abi.borrow(self), x, min_y, max_y, z, state_id });
    }

    /// Fills a 3D cuboid with a block state ID.
    pub fn fillCuboid(self: ChunkBuffer, min_x: u8, min_y: i32, min_z: u8, max_x: u8, max_y: i32, max_z: u8, state_id: u16) void {
        return abi.call(raw.@"[method]chunk-buffer.fill-cuboid", void, .{ abi.borrow(self), min_x, min_y, min_z, max_x, max_y, max_z, state_id });
    }

    /// Sets the biome at local coordinates (0..16, y, 0..16).
    pub fn setBiome(self: ChunkBuffer, x: u8, y: i32, z: u8, biome: Biome) void {
        return abi.call(raw.@"[method]chunk-buffer.set-biome", void, .{ abi.borrow(self), x, y, z, biome });
    }

    /// Fills the entire chunk with the given biome.
    pub fn fillBiome(self: ChunkBuffer, biome: Biome) void {
        return abi.call(raw.@"[method]chunk-buffer.fill-biome", void, .{ abi.borrow(self), biome });
    }

    /// Keeps the handle alive past the current callback. Release it with `deinit`.
    pub fn keep(self: ChunkBuffer) void {
        abi.keep(ChunkBuffer, self);
    }

    /// Releases the handle now instead of when the callback returns.
    pub fn deinit(self: ChunkBuffer) void {
        abi.drop(ChunkBuffer, self);
    }
};

/// A block's registry name and its property key-value pairs for a state ID.
pub const BlockStateInfo = struct {
    name: []const u8,
    properties: []const struct { []const u8, []const u8 },
};

/// Resolves a block name and optional properties to a block state ID.
/// Returns none if the block name is unknown.
pub fn resolveBlockState(name: []const u8, properties: []const struct { []const u8, []const u8 }) ?u16 {
    return abi.call(raw.@"resolve-block-state", ?u16, .{ name, properties });
}

/// Resolves a block state ID back to its block name and properties.
/// Returns none if the state ID is unknown.
pub fn blockStateToInfo(state_id: u16) ?BlockStateInfo {
    return abi.call(raw.@"block-state-to-info", ?BlockStateInfo, .{state_id});
}

/// Gets a block type by its numerical block ID.
pub fn getBlockById(id: u16) ?Block {
    return abi.call(raw.@"get-block-by-id", ?Block, .{id});
}

/// Gets a block type by its namespaced name (e.g., "minecraft:stone" or "stone").
pub fn getBlockByName(name: []const u8) ?Block {
    return abi.call(raw.@"get-block-by-name", ?Block, .{name});
}

/// Gets all registered blocks.
pub fn getAllBlocks() []const Block {
    return abi.call(raw.@"get-all-blocks", []const Block, .{});
}

/// Gets the names of all registered blocks.
pub fn getAllBlockNames() []const []const u8 {
    return abi.call(raw.@"get-all-block-names", []const []const u8, .{});
}

/// Gets the total number of registered block types.
pub fn getBlockCount() u32 {
    return abi.call(raw.@"get-block-count", u32, .{});
}

/// Gets the total number of registered block states.
pub fn getBlockStateCount() u32 {
    return abi.call(raw.@"get-block-state-count", u32, .{});
}

/// Gets all valid block states for a block type.
pub fn getStatesForBlock(block: Block) []const BlockState {
    return abi.call(raw.@"get-states-for-block", []const BlockState, .{block});
}

/// Gets all valid block states for a numerical block ID.
pub fn getStatesForBlockId(block_id: u16) []const BlockState {
    return abi.call(raw.@"get-states-for-block-id", []const BlockState, .{block_id});
}

/// Gets all valid state IDs for a numerical block ID.
pub fn getStateIdsForBlockId(block_id: u16) []const u16 {
    return abi.call(raw.@"get-state-ids-for-block-id", []const u16, .{block_id});
}

/// Gets property key-value pairs for a numerical block state ID.
pub fn getBlockProperties(state_id: u16) []const struct { []const u8, []const u8 } {
    return abi.call(raw.@"get-block-properties", []const struct { []const u8, []const u8 }, .{state_id});
}

/// Gets the block type associated with a block state ID.
pub fn getBlockFromStateId(state_id: u16) ?Block {
    return abi.call(raw.@"get-block-from-state-id", ?Block, .{state_id});
}

/// Gets the block type associated with a block state.
pub fn getBlockFromState(state: BlockState) Block {
    return abi.call(raw.@"get-block-from-state", Block, .{state});
}

/// Gets the default block state for a block type.
pub fn getDefaultStateFromBlock(block: Block) BlockState {
    return abi.call(raw.@"get-default-state-from-block", BlockState, .{block});
}

/// Gets the default block state for a numerical block ID.
pub fn getDefaultStateFromBlockId(block_id: u16) ?BlockState {
    return abi.call(raw.@"get-default-state-from-block-id", ?BlockState, .{block_id});
}

/// Gets the detailed block state for a numerical block state ID.
pub fn getBlockStateById(state_id: u16) ?BlockState {
    return abi.call(raw.@"get-block-state-by-id", ?BlockState, .{state_id});
}

const raw = struct {
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-id"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-uuid"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-type"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-position"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-world"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-yaw"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-pitch"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-head-yaw"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.teleport"(i32, f64, f64, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-velocity"(i32, f64, f64, f64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-velocity"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-pose"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-name"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-custom-name"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-custom-name"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-custom-name-visible"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-custom-name-visible"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-invulnerable"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-invulnerable"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-fire-ticks"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-fire-ticks"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-fall-distance"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-fall-distance"(i32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-ticks-lived"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-ticks-lived"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-sneaking"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-sneaking"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-sprinting"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-sprinting"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-swimming"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-swimming"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-invisible"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-invisible"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-glowing"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-glowing"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-fall-flying"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-fall-flying"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-silent"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-silent"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.has-gravity"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-has-gravity"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-width"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-height"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-rotation"(i32, f32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-on-ground"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-on-fire"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-on-fire"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.has-visual-fire"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-visual-fire"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-portal-cooldown"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-portal-cooldown"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-remaining-air"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-remaining-air"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-max-air"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-eye-height"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-eye-position"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-nearby-entities"(i32, f64, f64, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-vehicle"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-vehicle"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-passengers"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.add-passenger"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.remove-passenger"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.eject-passengers"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-bounding-box"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-in-water"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-in-lava"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.remove"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.raycast"(i32, f64, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.ray-trace-block"(i32, f64, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.ray-trace-entity"(i32, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-target-entity"(i32, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.set-custom-data"(i32, i32, i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.get-custom-data"(i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.remove-custom-data"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.has-custom-data"(i32, i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.as-living"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.as-mob"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-living"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]entity.is-mob"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.as-entity"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.as-mob"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.is-mob"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.get-health"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.set-health"(i32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.get-max-health"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.set-max-health"(i32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.damage"(i32, f32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.is-dead"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.get-absorption"(i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.set-absorption"(i32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.get-attribute-value"(i32, i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.get-attribute-base"(i32, i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.set-attribute-base"(i32, i32, f64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.add-attribute-modifier"(i32, i32, i32, i32, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.remove-attribute-modifier"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.get-attribute-modifiers"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.reset-attribute"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.reset-all-attributes"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.get-equipment"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.set-equipment"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.clear-equipment"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.get-age"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.set-age"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]living-entity.send-system-message"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.as-entity"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.as-living"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.add-ai-goal"(i32, i32, i32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.add-custom-ai-goal"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.clear-ai-goals"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.set-ai-disabled"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.is-ai-disabled"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.set-target"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.get-target"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.navigate-to-pos"(i32, f64, f64, f64, f64) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.navigate-to-entity"(i32, i32, f64) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.stop-navigation"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.is-navigating"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.has-reached-destination"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.set-navigation-speed"(i32, f64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.can-reach"(i32, f64, f64, f64, f32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.set-pathfinding-malus"(i32, i32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.get-pathfinding-malus"(i32, i32) f32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.look-at"(i32, f64, f64, f64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.look-at-entity"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.get-mob-data"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.set-mob-data"(i32, i32, i32, i32, i64, i64, i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.set-freeze-ticks"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]mob.get-freeze-ticks"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-x"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-z"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-block-state-id"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-block-state"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.set-block-state"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-block"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.set-block"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.set-block-by-id"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-biome"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-block-entity"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-top-block-y"(i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-sky-light"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-block-light"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.set-custom-data"(i32, i32, i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.get-custom-data"(i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.remove-custom-data"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk.has-custom-data"(i32, i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-center-x"(i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-center-z"(i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-center"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-center"(i32, f64, f64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-diameter"(i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-size"(i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-diameter"(i32, f64, i32, i64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-size"(i32, f64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-size-transition"(i32, f64, i64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-target-diameter"(i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-target-speed"(i32) i64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-warning-distance"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-warning-distance"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-warning-delay"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-warning-delay"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-warning-time"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-warning-time"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-damage-buffer"(i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-damage-buffer"(i32, f64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.get-damage-amount"(i32) f64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.set-damage-amount"(i32, f64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.contains"(i32, f64, f64) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.contains-pos"(i32, f64, f64, f64) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world-border.reset"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-id"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-border"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-world-border"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-chunk"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-block-state-id"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-block-state"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-block-state"(i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-block"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-block-id"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-block"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-block-by-id"(i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-block-by-name"(i32, i32, i32, i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-time-of-day"(i32) i64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-time-of-day"(i32, i64) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-world-age"(i32) i64;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-dimension"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-spawn-location"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-top-block-y"(i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-motion-blocking-height"(i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.is-raining"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-raining"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.is-thundering"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-thundering"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.broadcast-system-message"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-scoreboard"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.play-sound"(i32, i32, i32, f64, f64, f64, f32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.play-custom-sound"(i32, i32, i32, i32, f64, f64, f64, f32, f32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.spawn-particle"(i32, i32, f64, f64, f64, f64, f64, f64, f32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.create-explosion"(i32, f64, f64, f64, f32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-sea-level"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-min-y"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-sky-light"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-sky-light"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-block-light"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-block-light"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-biome"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.spawn-entity"(i32, i32, f64, f64, f64) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-entities"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.strike-lightning"(i32, f64, f64, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.ray-trace-blocks"(i32, f64, f64, f64, f64, f64, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.ray-trace-block"(i32, f64, f64, f64, f64, f64, f64, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.ray-trace-entity"(i32, f64, f64, f64, f64, f64, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.ray-trace-entities"(i32, f64, f64, f64, f64, f64, f64, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-block-entity"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-block-entity-nbt"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-block-entity-nbt"(i32, i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-name"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.save"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-chunk-generator"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-custom-data"(i32, i32, i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-custom-data"(i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.remove-custom-data"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.has-custom-data"(i32, i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.get-game-rule"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]world.set-game-rule"(i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.get-x"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.get-z"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.get-min-y"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.get-height"(i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.set-block-state-id"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.get-block-state-id"(i32, i32, i32, i32) i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.fill-layer"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.fill-range"(i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.fill-cuboid"(i32, i32, i32, i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.set-biome"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[method]chunk-buffer.fill-biome"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"resolve-block-state"(i32, i32, i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"block-state-to-info"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-block-by-id"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-block-by-name"(i32, i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-all-blocks"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-all-block-names"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-block-count"() i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-block-state-count"() i32;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-states-for-block"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-states-for-block-id"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-state-ids-for-block-id"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-block-properties"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-block-from-state-id"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-block-from-state"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-default-state-from-block"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-default-state-from-block-id"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"get-block-state-by-id"(i32, i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[resource-drop]entity"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[resource-drop]living-entity"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[resource-drop]mob"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[resource-drop]chunk"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[resource-drop]world-border"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[resource-drop]world"(i32) void;
    extern "pumpkin:plugin/world@0.1.0" fn @"[resource-drop]chunk-buffer"(i32) void;
};
